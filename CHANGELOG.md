Warning: truncated output (original token count: 30877)
Total output lines: 1122

# FocusFlow 版本记录

## 8.3.0-rc.13

- 外观设置与自定义主题编辑器新增共享的玻璃卡面不透明度调节，亚克力和毛玻璃均可设为 40%–95%；卡片、弹窗和悬浮底栏同步使用该值。
- 旧设置及旧主题预设继续使用原亚克力 60%、毛玻璃 48% 默认效果；页面背景和课表底板各自的不透明度设置不变。
- versionCode 547；延续 rc.12 的课表截图识别防错范围。自动验证通过，等待 OPPO / ColorOS 真机验收；尚未合并、未发布。

## 8.3.0-rc.12

- 课表截图识别明确按顶部星期表头和左侧节次坐标定位课程；星期或节次缺失、越界、非整数时不再被默认或钳制到第一格，并会提示被跳过的条目。
- 当多数条目缺少可靠坐标，或三门不同课程明显塌缩到同一星期和节次时，整批结果停止导入，避免看似完整的错误课表进入待确认列表。
- 多门不同课程占用完全相同星期与完整节次时，关闭直接“确认”，要求逐门“编辑并确认”；设置页同步说明复杂网格、拍屏摩尔纹与透视图片的模型限制。
- versionCode 546；已被 rc.13 替代。尚未合并、未发布。

## 8.3.0-rc.11

- 收集箱、逐步推进和参考卡片统一占满内容宽度，长标题或备注只在卡片内换行，不再改变卡片宽度。
- 修复加号弹窗经底部导航切换后被历史系统收起、再次点击加号不显示的问题；每次点击会以新的请求序号重新注册弹窗。
- 课程编辑只保留统一弹窗的一层滚动，地点选项改为自适应换行，修复长地点名导致的异常大块空白；所有页内弹窗的底部按钮允许换行，降低窄屏或大字体挤压风险。
- 上一步与下一步图标在现有位置上继续向左右两侧各移动 2dp。
- versionCode 545；rc.10 已被本候选替代。其余用户反馈已整理到 `docs/8.3.0-feedback-plan.md`；尚未合并、未发布，等待 CI 与 OPPO 真机验收。

## 8.3.0-rc.10

- 图片背景改为在首个 Compose 树建立前于 IO 线程完成解码，并把同一张位图直接作为首屏初值复用；不再先显示主题底色、随后再次解码并触发整页背景与全部亚克力卡片重建。图片尺寸与既有导入上限不变，不以降低画质换流畅度。
- 隐藏页签预热从“启动 1.2 秒后连续组合三页”改为交互感知：任何首次按下都会取消当前等待并重新计算空闲窗口，每个窗口只组合一个完整页签；图片背景与亚克力的 Haze 效果层不再集中建立。
- 启动后的提醒恢复移到 IO 线程；活动会话与历史沿用启动快照，首轮不重复读取，后续轮询也在 IO 读取后才回主线程更新状态。
- versionCode 544；rc.9 已被本候选替代。CI 门禁与 OPPO / ColorOS 图片背景＋亚克力真机帧数据以本候选运行结果为准；尚未合并、未发布。

## 8.3.0-rc.9

- 浙江大学教务导入页从临时的系统控件纵向布局迁移为 Compose：跟随 FocusFlow 当前主题与深色模式，使用统一圆角卡片、清晰的信息层级和全宽主操作按钮。
- 账号密码区增加密码显示／隐藏与键盘完成操作；“凭据仅用于本次导入”独立展示。提交后密码仍立即从界面清除，失败时保留账号、只需重新输入密码。
- 导入进度以七个阶段呈现：连接认证、加密凭据、验证账号、建立教务会话、读取学期、获取课表、解析课程；已完成、当前、未开始和失败状态分别显示，错误直接附在对应步骤下方。
- 页面补齐状态栏、安全区、键盘避让、纵向滚动、窄屏和大字体适配；运行期间锁定输入与返回操作，避免重复提交。
- versionCode 543；rc.8 已被本候选替代。代码、UI 状态单测、Debug/Release 构建与签名校验已通过；尚未合并、未发布，仍需 OPPO 真机核对实际排版、键盘和导入流程。

## 8.3.0-rc.8

- 修复同一手机浏览器可以打开浙大统一身份认证、FocusFlow 却持续停在“正在验证账号”并超时的问题：认证提交与进入教务系统拆成两个进度阶段，到达教务域名并取得会话 Cookie 后不再等待无用的完整页面正文。
- CAS 登录请求补齐语言、来源与浏览器导航请求头，并提交认证页的“记住登录”字段；认证响应上限仅对该阶段适度放宽，不拉长其他课表请求。
- 超时提示现在区分连接认证、验证账号、建立教务会话、读取学期和下载课表。密码提交不会自动重试，避免网络抖动造成重复认证或触发账号风控；凭据与 Cookie 仍不持久化、不写日志。
- versionCode 542；rc.7 已被本候选替代。代码、单元测试、Debug/Release 构建与签名校验已通过；尚未合并、未发布，仍需 OPPO 真机用实际账号复测认证、课表和速度。

## 8.3.0-rc.7

- “权限与提醒”中心继续集中展示应用通知、任务／饭点通知渠道、精确闹钟、电池优化，以及 ColorOS 横幅、自启动和后台策略指引；无法可靠读取的厂商开关明确标为“需手动确认”。
- 浙江大学课表入口调整为“从教务网导入 → 浙江大学”：应用内提供账号和密码输入框，原生 HTTPS 流程自动完成 CAS 登录、读取当前学年／学期并获取学生课表，不再要求用户在完整网页中手动导航。
- 导入页显示连接、加密、认证、读取学期、获取课表、解析等确定进度。密码使用当前 CAS 公钥加密，只存在于本次内存流程并在结束时清除；账号、密码与会话 Cookie 均不持久化、不写日志，不绕过验证码或账号解锁。
- 浙江大学新课程进入待确认；课程名、星期和节次可唯一匹配时更新已有课程的节次、地点与校区，同时保留其 ID、确认状态、启停状态和生效期；同名多节等歧义项只新增候选，不覆盖，不因课表缺项删除本地课程。截图与手动导入维持原有待确认行为。
- 首次启动的本地设置、课程、任务和历史继续由 IO 线程一次生成快照；跳过首次 ON_START 重读，并把隐藏页签预热延后到首屏稳定后。
- versionCode 541；rc.6 已被本候选替代。尚未合并、未发布，仍需浙江大学账号真机登录、验证码／账号异常分支、实际课表样例与 OPPO / ColorOS 帧耗时验收。

## 8.2.2-rc.4

- 步行、自行车和电动车可分别设置校内通勤的路上预留时间；路线实测学习仍优先，楼内进出缓冲保持独立。
- 修复玻璃卡片透明容器在深色主题下可能回退黑色正文的问题，统一使用主题正文色。
- 权限入口始终位于今日页“开始活动”下方；即使权限已设置也可查看。确认不再提示后，仅在设置页“检查更新”上方保留单列入口。
- 权限入口点开后分为必要的通知权限和可选的精确闹钟，并标注各自影响；可恢复今日提示。

## 8.2.1-rc.7（亚克力与毛玻璃扩展）

- 玻璃卡片不再叠加 Material elevation；撤回导致页面无法上下滑动的 CLAMP 硬件模糊层。
- 修复计划页“从结果开始”卡片在玻璃材质下出现厚灰色圆框和内层直角矩形：移除独有的深色底层与 elevation，统一为 20dp 圆角；操作强调继续由“新增目标”按钮承担。
- 修复计划页从“从结果开始”区域起的卡片边缘冲突：玻璃材质不再继承入口卡片的通用描边，亚克力无边，毛玻璃只保留自身圆角亮边。
- 恢复“默认设置怎么理解”折叠卡片并压缩为一行说明；继续缩短设置入口、材质说明与更新提示。

- 卡片材质扩展为五档：默认／渐变／柔光／亚克力／毛玻璃。
- 亚克力重画卡片所覆盖的页面背景并使用 18dp 模糊，保留较多背景轮廓，再叠加 16% 主题染色和 60% 面层；不画独立描边，靠染色与模糊自然成边。
- 毛玻璃使用 30dp 背景扩散、48% 低染色白纱、窄顶部高光与沿真实圆角闭合的 1dp 亮边；亚克力不画独立描边。
- 去掉底栏与弹窗在玻璃材质下的通用边框，避免亚克力残留边线、毛玻璃出现双重描边。
- 两种玻璃材质都只模糊表面底层，不模糊文字与按钮；悬浮底栏和页内弹窗新增共享页面捕获源，能按实际位置透出并模糊被遮住的日程文字、色块和背景，而非只画一层假透明色。
- 页面捕获只在「丰富外观」开启且选择亚克力／毛玻璃时启用，并以 0.66 倍输入采样控制离屏绘制成本；默认关闭路径不创建捕获层。
- 存储仍使用既有 `card_material` 键；新增 `frosted` 值，旧值与未知值均按原兼容规则读取，不清理用户数据。
- 「丰富的动画与外观效果」继续默认关闭；关闭时玻璃材质不绘制但选择会保留，重新开启即可恢复。
- 候选版本为 `8.2.1-rc.7` / versionCode 532，正式版仍为 8.2.0。

## 8.2.0（可选外观系统）

- **页面背景四档**（设置 → 外观 → 页面背景）：跟随主题／主题渐变／固定颜色／自选图片（可调不透明度）。图片只写进应用私有目录、不上传，导入时按屏幕尺寸降采样；图上始终压一层主题遮罩保正文对比度。
- **渐变可调**：自选配色（八组「顶色→底色」，不选则按主题派生）、强度 0–200%（0% 即纯色）、以及「跟随内容」——整条渐变铺数屏、每屏只截一小段，竖向颜色变化更慢更缓。默认外观与 8.1.1 逐像素一致（真机 A/B 实测 meanAbsDiff = 0）。
- **从图片抽取主题色**：色相分桶 + 圆均值（红色跨 0° 不会被平均成青色），派生完整配色并保证正文对比度；画面太素时如实返回"没有足够明显的颜色"且不改动主题。
- **卡片材质**：默认／渐变／柔光／纸感。抽成统一卡片组件 `FocusCard`，51 处卡片容器已收编 32 处（收编后默认材质渲染路径与原来逐字相同）；纸感噪点为程序生成的 64×64 贴图，不增加安装包体积。剩余 19 处（`ElevatedCard`/`OutlinedCard`/带描边或点击重载的卡片，以及有意保留的课表底板）待真机 A/B 后再动。
- **课表与日程表底色**：跟随主题／选颜色（八组浅色预设）／图片。**只改底板**，课程块与日程块颜色来自各自数据，未做任何改动。
- **新增三套主题**：石墨、樱粉、青竹。原有四套（海盐蓝／薄荷绿／暖杏／暮紫）取值冻结，并以金值 + 对比度断言回归；新主题另加"与其它主题可区分（通道差 ≥6）"的硬要求。
- **自定义主题对比度体检**：编辑器当场列出六对关键前景/背景的对比度，不达标标红并指出是哪一处。
- **自定义主题工具升级**：顶部新增**实时预览**（用应用里同一套渲染画迷你页面：背景、渐变、卡片材质、底栏，所见即所得，不是另画一套示意图）；背景与卡片外观、课表底色直接在工具里调（与「设置 → 外观」共用同一批控件，只有一份实现，不会长出两套长得不一样的界面）；“从图片抽取主题色”在工具里只更新工作副本，确认后才应用；保存预设时可勾选“同时记住当前外观”，预设从一个配色变成一整套外观，点“应用”整套切换；**老预设（只存了配色）行为完全不变**——应用它不会动当前的背景与卡片；“恢复默认”改为连外观一并复位。
- **深色模式适配**：渐变另配一套幅度（顶 +10% 白、底 −25% 黑），自选的浅色配色与固定背景色按 22% 权重压到深色底上（保留色相、整页仍是深色）；单测断言深色下渐变各点亮度 < 0.35、正文对比度 ≥ 7:1。
- 底栏两侧箭头继续向角落收（触控框 38dp / 图标 28dp，偏移 −2dp / 0dp）；底栏描边由 2dp 灰线改为 0.6dp 发丝线 + 更柔的边缘过渡；「上一步／下一步」出现时的底栏顶角圆角由 10dp 调到 18dp（不再那么方，仍与平时 46dp 的完整胶囊有明显形变对比）。
- **默认外观与 8.1.1 逐像素一致**：同机来回安装两个包、同一页面比对，`meanAbsDiff 0.37`（阈值 ≤1.0）；其中**页面上半部分逐像素为 0.00**，差异全部来自本版要求的底栏描边改款（2dp 硬灰线 → 0.6dp 发丝线 + 更柔的阴影过渡）。原有的四套主题取值、课程块与日程块颜色、偏好键与预设 JSON 形状全部未动。
- 单元测试 575 条 / 95 个测试类全绿；versionCode 522 / 8.2.0。

## 8.1.1（上一步／下一步与弹窗的层级关系修订）

- 修「上一步／下一步」把弹窗当成独立一步：按维护者口径，「副页 + 弹窗 + 已填数据」算**同一个整体**，打开弹窗本身不计入历史。于是开着弹窗按上一步＝回到上一个页面（例如从「课程 + 编辑弹窗」回到计划主页）；反过来，从这里跳到别处（管理地点／其他页／本页签主页）之后再按上一步，副页、弹窗和里面的内容一起回来。用户自己关掉弹窗后，栈里那些整体快照**降级**成"只有页面"——页面这一步要留着，上一步不会因此多退一步。
- 弹窗卡片改用主题染色的柔和阴影（20dp）+ 顶面高光 + 1px 细描边，明显浮在压暗的页面之上（真机实测：卡片下沿 130px 内亮度 147.8→160.5 的柔和过渡）。
- 浅色模式页面底色比卡片压深 5%，卡片与页面的分层一眼可见（暖杏下页面 `#F2ECE8` / 卡片 `#FFFBF9`）；四套主题正文对比度仍为 13.9–14.2:1。
- 修收起状态下的弹窗仍会挡住页面点击（悬浮层不再留下 pointerInput 节点）；新弹窗接管时把已收起的旧弹窗真正关掉。
- 修「回本页签主页」会把这个整体吃掉：A→B→A 去抖规则加了例外——B 上开着弹窗时不再抵消。真机复现：课程 + 编辑弹窗 → 点「计划」页签回主页 → 上一步再也回不去；修好后上一步整包还原。
- 单元测试 515 条 / 88 个测试类全绿（新增「整体语义」导航用例、外观偏好模型、WCAG 对比度守卫、主题色抽取）。
- versionCode 521；8.1.0 保持已发布状态，本版是它的修订版（维护者指示直接发出，未走 rc 候选流程）。

## 8.1.0（统一动效规范与深度缩放转场）

- 新增统一动效规范 MotionSpec：进入 240ms／退出 170ms／位移 260ms／底栏形变 260ms／副页跨页签 320ms 的语义化时长与缓动集中管理，全部经「设置 → 外观 → 动画速度」倍率换算；速度设为「关闭」时所有转场瞬时完成。
- 副页转场改为深度缩放：从底栏对应图标放大展开，返回时缩回同一图标并同步淡出，主页退到 0.96 当底层；主页与主页之间一律左右平移（普通 80dp、跳转 96dp），不再缩放；返回时不再重播收起动画。
- 底栏选中底色改为会平移的色块：从上一个页签滑到当前页签，进入子页时改为圆环，标签交叉淡入替换为子页名；回退／折返仍为两端圆瓣形变。
- 深链／通知／快速记录／更新说明跳转**直接打开**子页时不再被当成「顺带重置」抑制，照常从底栏图标放大展开；只有把目标页签的子页**收回主页**时才不补播动画。
- 转场判定抽为纯函数 TabMotionRules 并补单元测试（484 条）；启动改为按需组合当前页签、首帧后逐帧预组合其余页签；今日／计划／主页的派生计算 memo 化。真机（1440×3168）release 包冷启动 188ms、切换 90 分位 11ms。
- 修复离开带子页的页签后约 400ms 内页面点击被静默吞掉；修复退出确认提示时长与二次返回时间窗不一致（提示回到 4 秒）；冷启动时系统返回补上兜底处理器，不再跳过二次确认直接退出；历史列表跳转补齐转场判定。
- 弹窗由独立窗口改为页内浮层（AppDialog）：弹窗打开时悬浮底栏（含回退／折返键）仍在最上层、可点，不再被系统窗口整层盖住；返回键、点遮罩关闭，点卡片空白处不穿透；键盘弹出时弹窗整体上移到键盘与底栏之上。53 处调用点统一迁移，外观与 Material3 AlertDialog 一致（28dp 圆角、24dp 内边距、同字体与按钮排布），进场／退场走 MotionSpec。
- 弹窗卡片改为从屏幕下方上下平移进出；任何页面导航（点入口、上一步／下一步、跳转、点 ＋）都先关闭弹窗，不再出现"页面切了、弹窗还浮在新页面上"；弹窗打开期间再开一个弹窗（如长按回退键的历史列表）由后开者接管，不会退回系统窗口。（弹窗打开时底栏的"压暗/柔光"处理仍在调整，本轮不包含。）
- 修复子页放大／缩小转场掉帧：转场期间整页每帧重组（连续 20–24ms）→ 重帧从 17.5% 降到 4.2%，均值 9.2ms。
- 修复横屏下弹窗底部按钮被底栏盖住（卡片避让底栏、按钮固定、正文滚动）；应用改为**锁定竖屏**，该几何作为大屏/分屏兜底保留。
- 重启恢复如实化：真机重启实测 ColorOS 会推迟开机广播（`DEFER_BY_OPLUS`），提醒只能等用户打开应用时补登记。设置页不再笼统写"重启后自动恢复"，改为一行短说明 + 必要时显示"本次开机系统推迟了开机启动"，完整路径与机制放进问号帮助与说明书。
- 验收改用稳定签名 release 包（此前手感数据来自 debug 包）；单元测试 484 条 / 84 个测试类。
- versionCode 520（rc.6 转正）；rc.1~rc.6 废弃。

## 8.1.0-rc.5（底栏形变、历史规则 v3 与检查更新整合）

- 回退／折返改为底栏两端圆瓣形变：缩放淡入、不挤压任何页签；长按回退键仍弹会话历史列表。
- 历史规则 v3：主页↔主页跳转不再记录；在主页发生实质数据操作时记为一步（回退键可找回工作现场）；保留 A→B→A 去抖与容量 30。
- 检查更新整合为独立子页并上移到三个文档入口上方：手动检查＋自动检查（同日去重）＋「接受候选版」勾选（默认关、需二次确认、仅稳定签名候选）。
- versionCode 518；rc.4 废弃。

## 8.1.0-rc.4（导航底栏整合、两级退出与检查更新）

- 回退／折返键整合进悬浮底栏两端：有上级／下级时横向展开并淡入，无历史时收起不占位；长按回退键弹出本次会话页面历史列表，点选直达并截断其后历史。
- 页面历史去抖：A→B→A 自动抵消（点错又点回不记历史）；容量上限 30。
- 两级退出：非今日页按返回先回今日主页（进历史）；今日页二次返回退出；退出提示可点「不再提示」永久关闭，设置 → 退出确认 开关可随时改回。
- 新增「检查更新」：手动检查＋可选自动检查（启动时静默、同日去重），数据源仅本仓库 GitHub Releases，只接受正式版（tag vX.Y.Z 且非 prerelease）；应用内下载 APK 后调系统安装。
- versionCode 517；rc.3 废弃。

## 8.1.0-rc.3（课程编辑器挂起恢复、草稿清空与浮动键重排）

- 课程编辑器跳转「管理地点与出行参数」后回到课程页自动重开，已填内容由草稿箱恢复；新增与编辑两种模式都覆盖。
- 新建类弹窗增加「清空」按钮（新增课程、新建目标、收集教程、改进想法、安排空闲活动、快速记录、视频分析）：一键回到空白表单并清掉草稿缓存；取消仍保留草稿。
- 回退／折返浮动键从底栏两端重叠位置改为悬浮于底栏圆角上方（8dp 间隙）并淡入淡出，不再遮挡导航栏；布局经 Qwen3-VL 视觉评审确认。
- versionCode 516；rc.2 废弃。

## 8.1.0-rc.2（更新说明修复）

- 修复应用内更新说明未展示 8.1 核心变化的问题：退出二次确认、回退／折返历史键与弹窗草稿恢复现会出现在覆盖安装后的更新提示中。
- 将“更新说明必须展示版本核心变化”写入仓库规则（AGENTS.md 与 VERSIONING.md 发布流程）。versionCode 515；rc.1 废弃。

## 8.1.0-rc.1（退出确认与页面历史）

- 根页面按系统返回不再直接退出：先弹“再按一次返回键退出应用”提示，提示消失前再次返回才真正退出；弹窗、子页等其他层级的返回行为保持不变。
- 会话内页面导航历史：回退／折返浮动键仅在有历史时显示于悬浮底栏两端，可跨页签回到刚才操作的页面并恢复当时的页面状态；历史仅本次运行内有效，新导航清空折返分支。
- 弹窗草稿保险箱：含输入的弹窗被误关（按返回／取消）后重开自动恢复正在填写的内容，保存成功后清理。覆盖收集箱编辑、快速记录、目标／课程编辑、活动与游戏安排、用餐记录、作息分组向导、课表节次表、主题／地点编辑、AI 工具等全部含输入弹窗。
- 新增导航历史单元测试（回退／折返／截断／容量／草稿箱读写）。versionCode 514。

## 8.0.0-rc.4（滚动七天空挡日期修复）

- 空挡图、空挡建议和新增目标的容量估算改为从今天起滚动七天；已经过去的日期不会继续占用下周同一星期。
- 今天已经过去的时间整体视为不可再安排，避免重新推荐过期时段；前一天跨午夜进入今天的有效尾段仍参与占用。
- 新增“昨天任务不阻塞下周同星期”和“今天已过时间不再推荐”回归测试。versionCode 512；Run 306 全部门禁通过并完成针对性验收，已并入正式版。

## 8.0.0（正式版）

- 整合今日状态、收集箱推进、固定课表、课程管理、地点与通勤、空挡建议、目标执行、历史统计和统一设计语言。
- 修复重叠课程或通勤边界导致计划页闪退；逾期事务明确显示时间并提供完成或重新安排入口。
- 任务、历史与目标计数使用原子提交和快照校验；返回前台同步刷新，避免旧页面覆盖通知操作。
- 排程按具体日期和跨午夜区间计算；暂停、完成和过去日期的任务不再错误占用未来空挡。正式版 versionCode 513；rc.4 已通过 Run 306 与针对性验收，正式提交通过独立完整门禁后发布。

## 8.0.0-rc.3（任务一致性与日期边界收束）

- 应用内任务写入增加存储快照校验；通知栏完成、最低版本、延后一小时和跳过会将任务、历史及目标计数一次提交，返回前台同步刷新任务、目标和活动状态，避免旧页面覆盖通知操作。
- 排程占用改为按具体日期计算并拆分跨午夜任务；过去同星期的任务不再阻塞今天或下周，今日摘要也不再混入上周同一天的安排。
- 重叠／嵌套课程始终保留最晚下课边界，不生成课程内部的虚假空挡或通勤；暂停任务清除原日程并停止占用。
- 自动目标与活动建议按未来七天的实际日期扣除任务和缓冲，批量自动安排的任务与历史一次保存。versionCode 510；Run 303 全部门禁通过并完成真机验收，已提升为 8.0.0。

## 8.0.0-rc.2（计划页稳定性与逾期提示）

- 修复重叠课程、连续课程或课间时长短于通勤估算时，计划页因反向时间区间而闪退；此类组合现在安全记为 0 分钟可用空挡。
- 计划首页仅统计至少 10 分钟的真实可用空挡，不再把 0 分钟间隔显示为可用数量。
- 几小时或几天前未完成的任务，…18877 tokens truncated…限改为首次启动时一站式申请（带回调，申请后自动跟进精确闹钟）；申请过一次不再重复打扰。习惯基线引导在权限弹窗期间不会叠加。
- 活动提醒页新增权限状态行：通知未开启时红字提示并可一键跳转系统设置，从系统设置返回自动刷新。
- 精确闹钟按设备支持情况处理：ColorOS（OPPO/realme/OnePlus）实测授权页无开关，自动放弃并改用普通后台提醒。
- 主题多色档位：配色档位可选 3 色常用（主操作/强调/日程）、4 色加背景、5 色加警示，外观预览与自定义槽位同步。
- 自定义主题：槽位按功能语义分工（主操作色管按钮开关、强调色管提示文字、日程色管日程色块、中性色管背景卡片、警示色管提醒警示），从 30 色预设色板点选，立即全局生效并记住；可一键恢复最近用过的内置主题。
- 四套内置主题均新增强调色（tertiary），用于今日页精力建议与"下一件合适的事"提示文字。

## 3.9.8

- 提醒通知全部升级为横幅弹出（像微信一样弹出几秒＋声音）：活动结束、结束预告、任务、状态询问、饭点、睡前减速均为高重要性渠道；温和版到点提醒为静音横幅（仍比普通提醒醒目）；旧低重要性渠道自动删除。

## 3.9.7

- 设置页枢纽化续：外观（主题选择）与活动提醒（开关、提前预告与延长上限）同样改为枢纽卡片＋副页面，与计划页一致；设置主页只保留短分区与枢纽卡片。

## 3.9.6

- 设置页枢纽化：通勤与地点、教程联网搜索两块由页内展开收起改为与计划页一致的枢纽卡片（标题＋摘要＋箭头），内容移入对应副页面，副页面内保留问号帮助；设置主页大幅缩短。

## 3.9.5

- 设置页与版本路线图箭头统一换为 Material 矢量图标，修复文字与箭头的垂直对齐问题。
- 教程联网搜索默认模型改为 Qwen/Qwen2.5-7B-Instruct（旧默认已从硅基流动下线）；已保存的旧默认值自动迁移，模型类错误提示附更换引导。

## 3.9.4

- 设置页折叠分区样式统一：去掉独立卡片背景，与页面其他分区一致的扁平标题行；问号紧贴标题、摘要与统一箭头“›”（与版本路线图同款，展开时旋转 90°）置于右侧，消除色差与突兀感。

## 3.9.3

- 设置页折叠分区卡片重做：通勤与地点、教程联网搜索整块收进圆角卡片，点击头部行带涟漪反馈与旋转箭头，内容平滑展开收起，收起时只占一行。

## 3.9.2

- 移除后台通勤记录功能：评估后认为出行方式固定假设、时段不区分导致路线学习数据质量差，投入产出比低；保留手动"记录本次耗时"路线学习。应用不再申请定位权限，也不包含前台定位服务。
- 设置页优化：通勤与地点、教程联网搜索两块默认收起（标题行显示状态摘要）；从子页面返回或切换标签后保留原滚动位置。
- 校园地点分区新增"其他"，自定义地点可归入；与其他分区之间用默认 12 分钟兜底估计，可手动校准、可被路线学习覆盖。
- 状态询问时刻的"数据充分时自动采纳建议"列入后续候选。
- POI 搜索准确度提升：改为以校区中心 3 公里为范围的周边搜索（按距离排序，结果标注距校园中心米数），空结果才降级全城搜索；全城文本搜索增加 citylimit 锁定城市。
- 校区中心可配置：设置页可改 POI 搜索的圆心（经纬度）与城市（默认浙大紫金港 · 杭州），其他学校可改为自己学校后使用搜索。

## 3.9

# FocusFlow 版本记录

## 3.9

- 校园地点三态：设置页新增“管理校园地点”，内置紫金港目录之外可本地增删改自定义地点（可填坐标）、可选填写高德 Web 服务 key 搜索校园 POI 一键加入；外部地点包（JSON）继续作为 DLC 覆盖内置目录。
- 后台通勤记录（可选）：设置页开启后，手动选择起点/终点开始记录；前台服务内用系统定位低频采样（约 60 秒/100 米，零 SDK 依赖），持续通知可见、可随时结束；未授权或定位不可用时自动降级为纯计时。结束确认后写入路线学习。
- 教程联网搜索（可选）：计划页“教程资料”可联网搜集候选教程并比较来源；复用设置页填写的硅基流动 key（仅存本机、仅发往 api.siliconflow.cn），无 key 时功能禁用。

# FocusFlow 版本记录

## 3.8.4

- 睡前减速卡新增“明日准备”：进入减速阶段后按明日具体安排生成摘要——明日课程数及最早一节课的名称与时间（无课则注明）、明日已安排任务标题（最多 2 项）、收集箱待整理项数。
- 日程表冲突课程色块移除边框，改为红色块内的斜向条纹警示；与相邻安排块按真实分钟数紧贴排布、无视觉间隔，以四角圆角区分前后两个安排。


## 3.8.2

- 日程表中冲突课程改为单一警示色块：同一时间段重叠的课程合并为一个覆盖整个冲突区间的色块（黄框红底白字），不再分成并排小色块，标题与详情列出各门冲突课程。


## 3.8.1

- 课程冲突显示统一：日程表中冲突课程改为黄底深红边框的统一警示色块（深浅主题均清晰），不再只是红框；计划页课程列表单独列出冲突课程并标注与哪门课重叠，可直接编辑修正；计划主页课程入口显示“⚠ N 门冲突”。
- 空档建议区分碎片时间：不足 10 分钟的空档单独列为碎片时间区块（仅够通行与缓冲，不参与安排建议），10 分钟以上仍显示在可安排空档中。


## 3.8

- 今日页精力卡片新增按时段（上午／下午／晚上）的精力规律与常见活动汇总，并结合当前时段给出建议；签到不足 3 次或时段样本不足时不出具体建议，不假装精确。
- 设置页状态询问区块新增建议询问时刻：按签到时间分布给出每日询问时间建议，可一键采纳。
- 计划页本周回顾升级为每周目标回顾：每目标显示本周进度条、近 4 周完成趋势（历史来自完成反馈记录）与低压力调整建议；页面顶部汇总本周累计完成。
- 睡前减速：每晚按习惯基线填写的睡觉时间提前 40 分钟提醒开始收尾，可在设置页关闭；今日页睡前 4 小时起显示减速提示，并按明天第 1–2 节是否有课给出“注意休息”或“可稍晚收尾”；过点熬夜时只给低压力建议。
- 本周回顾页新增长期反馈分析：积累 5 条完成反馈后汇总最常见阻碍、难度分布与最低版本占比，并结合最近两周变化给出心态与安排调整建议。
- 设置页新增个人账目区块：统计填写的消费草稿金额合计、本月支出、分类金额、常去地点与餐次分布；只统计手填金额，不自动记账或推断。


## 3.6

- 课表识别不再依赖 Google Play 服务：无 GMS 的设备自动改用随 APK 打包的本地识别模型，课程名称、星期与节次照常解析为待确认课程；有 Google Play 服务的设备仍使用识别质量更高的在线模型。
- 课表导入新增防冲突：识别结果与已确认课程时间冲突时拦截导入并列出冲突明细；识别结果内部互相重叠也会提示核对；确认或忽略课程后导入提示自动清空。
- 日程时间轴上时间重叠的课程块以红色警示边框标出，便于发现冲突课程。

## 3.5

- 设置页新增“版本路线图”副页面：显示当前版本、已实现版本演进（1.0 → 3.4）、3.5 计划与后续候选；不再提供候选勾选，改为纯展示。
- 全应用新增帮助问号：今日、日程、计划、设置页的功能说明收敛到标题右侧的圆形“?”入口；设置页每个功能区块标题右侧各有一个问号，正文保留操作引导。
- 版本演进数据来源于本文件，发版时需同步更新 RoadmapData.kt；CI 构建产物文件名改为自动读取 versionName。

## 3.4

- 结束用餐时补齐消费草稿字段：地点（可快捷选择宿舍／食堂／外卖／便利店，也可自由填写）、餐饮分类（食堂／外卖／自己做饭／便利店／其他）、商家或食物名、支付方式（微信／支付宝／校园卡／现金／其他），与原有金额、评价、备注一起保存。
- 今日餐点卡片显示每餐“常去／上次在”的地点提示，来自最近一次确认地点；就餐记录列表完整展示地点、分类、商家、支付方式等草稿字段，仍可随时删除。
- 所有草稿字段只保存在本机、不自动生成账目，为后续版本的个人账目分析保留原始数据。
- 3.4 之后的地图与后台定位仍按计划在后续版本加入，需要地图 SDK 密钥并在真机验证。

## 3.3

- 今日页新增“今日餐点”卡片：按当前生活阶段与星期显示每餐的预计时间，临近预测点时提示“准备吃饭？”，可随时提前记录“现在吃”。
- 饭点学习只使用你确认的开始与结束时间，按“生活阶段 × 星期 × 餐次”分组取最近 8 次的中位数；不足 3 次时只用你填写的基线，并明确标注“数据不足”，不假装精确预测。
- 提醒流程：预测点附近通知“准备吃饭？”（已在吃／稍后 20 分钟／今天不需要）；确认“已在吃”后按个人时长估计结束时间，到点通知“吃完了吗？”（吃完了／还在吃／不记录）。
- 设置页新增“饭点学习”区块：饭点提醒总开关、每餐当前学习到的规律与样本数、就餐记录查看与删除入口。
- 结束用餐时可选择记录消费草稿（金额、评价、备注），为 v4 理财预留字段；不记录不会被视为没吃，也不会写入学习数据。
- 就餐记录与消费草稿均只保存在本机；重建习惯基线不会清除就餐记录，原始数据始终可查看、修正或删除。

## 3.2

- 首次打开会进行约 2–3 分钟、可随时跳过的习惯基线引导：选择当前生活阶段（假期／上学／考试周）、大致起床与睡觉时间、各餐开始时间与时长，以及常见娱乐时段。
- 原始事件按时间追加保存且不因学习而覆盖：活动开始／结束／跳过、任务安排／改期／完成、精力签到、通勤耗时确认、作息与餐次锚点、生活阶段切换都会留下记录。
- 设置页新增“习惯基线”区块：随时查看当前基线、回到引导修正、浏览原始事件记录，或一键重建基线（会清空全部原始事件）。
- 假期与上学节奏分开保存，后续饭点与作息学习按“生活阶段 × 星期 × 餐次”进行；数据不足时只给出宽松提醒。
- 本轮只保存经用户确认的数据结构与事件流，不进行任何自动推断；系统推测的内容会明确标记为待确认，不写入学习数据。

## 3.1

- 校园路线不再被单次“校正值”永久覆盖；每次实际耗时作为一条用户确认记录保存。
- 同一出行方式与分区组合最多保留最近 12 次记录，并使用中位数更新课程空挡、目标候选时间与路线预览。
- 路线预览显示确认次数和最近五次耗时，便于识别异常数据。
- 新增撤销最近记录和清除该路线学习；清除后立即恢复内置保守估计。
- 自动迁移旧版单次校正值，将其作为第一条确认记录继续使用。
- 3.2 将在地图坐标可用后加入可选后台定位辅助记录；默认关闭、持续通知可见，并可随时停止。

## 3.0

- “开始活动”新增可解释的建议时间，并显示采用建议后的预计结束时刻。
- 建议依次参考最近同名活动、同类活动、任务原有预计时长和类别保守默认值；历史用时采用中位数，降低偶发超长或过短记录的影响。
- 当前精力偏低时会建议更容易开始的短版；充足或正常时不会擅自延长用户原定任务。
- 若下一项课程或固定任务临近，建议会保留 15 分钟缓冲；剩余时间不足时明确提示先准备下一项或只做五分钟短版。
- 建议仅提供“采用建议时间”按钮，用户仍可自由填写时长或结束时刻，不会自动开始活动。
- 将 3.1 规划为基于用户确认记录的通勤时间学习，不使用静默定位推断。

## 2.9

- 设置中新增可关闭的“每日低打扰询问”，可选择 08:00–22:00 的询问时刻和 30／60／120 分钟的主动推迟时长。
- 状态询问同时记录精力与当前活动；今日页显示最近记录，并保留随时手动签到的入口。
- 通知可直接打开签到或选择稍后；没有回应时当天不连续追问，已有活动进行中则自动退避到活动结束以后。
- 签到记录仅保存在本机，当前只更新弹性推荐所用的精力状态，不会自动移动固定日程。
- 为 V3 保留最多 365 条经用户确认的签到历史，后续学习时段精力和常见活动时不使用未确认推断。

## 2.8

- 收集箱“安排时间”升级为推荐空档、大致时间和精确时间三种方式。
- 推荐空档复用现有排程能力，避开已确认课程和未完成的定时任务，并保留 15 分钟缓冲。
- 大致时间支持今天下午、今天晚上、明天上午、明天下午和本周内等范围，不伪装成精确时刻。
- 弹性时间范围作为独立字段保存在本机；后续初步安排只会在该范围内寻找候选。
- 日程页会显示弹性范围，并可重新调整范围或确认具体候选；只有确认具体时间后才创建提醒。
- 开发计划加入 2.9 低打扰状态签到，以及 V3 基于用户确认记录的时段精力与活动学习。

## 2.7

- “下一件合适的事”从只读建议升级为可执行入口，可直接开始、改时间或确认完成。
- 目标任务显示“最低版本”入口，按目标预计时长生成 5–15 分钟的低压力活动预填。
- 当当前精力偏低，或近期反馈集中在“时间不够／精力不足”时，推荐理由会明确提示最低版本。
- 从推荐开始活动会预填名称、类别、预计时长和下一项固定安排；开始前仍可修改。
- 开始活动不会自动把任务标记完成，活动结束后仍由用户明确确认，避免误记完成情况。

## 2.6

- 课程副页面新增课表截图导入，使用设备端中文文字识别，不上传课表图片。
- 识别结果只生成“待确认课程”，不会直接写入周日程或参与空挡计算。
- 结合截图中的星期列、节次行、课程名称与教学楼生成草稿，并过滤已有课程，避免重复导入。
- 待确认课程新增“编辑并确认”，方便修正识别出的名称、节次和教学楼；原有手动新增继续保留。
- 对不含课程文字、星期或节次的截图给出明确提示，不以猜测结果替代确认。

## 2.5

- 日程页的弹性任务新增“初步安排”，在未来七天内生成最多三个可确认的候选时间。
- 候选时间结合任务预计时长、已确认课程、已有定时任务和当前精力，并为固定安排前后保留 15 分钟缓冲。
- 只有用户点选候选时间后才会写入日程和创建提醒；保持弹性不会改变任务。
- 初步安排会标记来源并保留原有改期能力，不把建议伪装成不可移动的预约。

## 2.4

- 今日页新增本地保存的精力状态：偏低时优先短任务，充足时优先较完整或目标相关的任务；固定日程与逾期提醒不被改写。
- 校园地点改为“应用自动提供优先”，紫金港用户无需制作地点包；JSON 导入收进高级迁移入口。
- 地点帮助中补充后续地图流程：自动搜索校区 POI、按类型推断用途，以及地图点选后的逆地理编码建议。
- V3 路线图加入自动校园 POI 发现和地图点选补充地点；真实地图 SDK 将等待开发者 Key、隐私告知和用户同意后启用。

## 2.3

- “今日”中的收集箱改为摘要加副页面入口；完整收集箱沿用计划副页面的无返回箭头、滑入淡入、系统返回和底栏状态反馈。
- 地点包标题旁新增圆形问号入口，说明 JSON 文件结构、分区代码、UTF-8 保存方式与系统文件选择器导入步骤。
- 路线预览可记录或更新实际总耗时，并可恢复初始估计；校正按出行方式和双向分区组合保存在本机。
- 课程空挡、目标候选时间和路线预览统一使用已确认的路线校正值，不读取后台定位。

## 2.2

- 设置页新增“校园生活”总开关；关闭时收起校内出行、地点包和手动位置工具，但保留课程与本地配置。
- 设置页新增校园地点包导入：接受经过校验的 JSON 文件，限制地点数量、校验分区并拒绝重复名称。
- 导入地点直接进入课程编辑器；地点所属分区继续参与课程空挡和校内路程估算，而非仅用于展示。
- 新增手动当前位置和路线预览；不申请系统定位权限，不后台追踪，也不自动改变日程。
- V3 路线图新增“开始活动建议预计时长／结束时间”，并明确通勤学习只使用用户确认的实际耗时。

## 2.1

- 从“开始活动”中移除不必要的“通勤”类别；通勤信息仍只用于课程空档和校内路程估算。
- “下一件合适的事”改为可解释推荐：依次检查刚错过的任务、90 分钟内的固定安排，以及能在下一项安排前完成并保留缓冲的弹性任务。
- 推荐卡直接显示选择原因；推荐只提供参考，不会自动开始、完成或移动任何任务。
- 下一阶段将加入手动精力状态、地点适配、最低版本与基于反馈的推荐校正。

## 2.0

- 在 1.8 信息架构与交互基础上整合 V2 活动模式，不恢复旧版导航和计划页。
- 开始活动时可选择类别、自定义名称、预计时长或结束时刻，并填写活动结束后的下一步。
- 今日页显示活动倒计时、预计结束时间、延长次数及当日活动记录。
- 到点后提供确认结束、开始下一步、限次延长和重新安排四类转场选择。
- 支持可调提前预告、到点提醒强度、连续延长上限及 Android 精确提醒权限入口。
- 活动与提醒状态写入本地存储，并在应用启动、系统重启或应用更新后恢复。
- 通知支持结束活动、延长十分钟、打开转场；重新安排会进入今日页内的收集箱。
- 设置页新增海盐蓝、薄荷绿、暖杏与暮紫四套主题；页面、导航、卡片、控件和日程色块会整体换色并记住选择。

## 1.8

- 移除日、周时间轴的当前时刻红线。
- 计划主页与副页面加入左右滑动淡入过渡，底栏计划项同步显示当前副页面。
- 计划副页面采用无返回文字和箭头的简洁标题；可通过系统返回键或再次点击底栏“计划”回到目录。

## 1.7

- 将底部导航调整为“今日、日程、计划、设置”，中央加号继续作为独立快速记录动作；收集箱整合进今日概览。
- 今日日程与今日概览分离，日／周时间轴集中到独立“日程”页。
- 计划主页改为无滚动的模块入口；课程、空挡建议、目标与执行、本周回顾、暂停项目进入各自可滚动副页面。
- 教程资料并入“目标与执行”，空挡建议从课程管理中独立出来。
- 周日程维持七天同屏且无横向滑动，同时压缩时间轴与间距、显著加宽色块。
- GitHub Actions 增加拉取请求编译验证，版本号提升到 1.7。

## 1.6

- 紧急修复底部中央加号容器撑满屏幕、导致所有页面内容不可见的问题。
- 保留 1.5 的计划页折叠、周日程适宽、06:00–24:00 时间轴和 V2 活动模式规划。

## 1.5

- 计划页条目移除“展开／收起”文字，只保留方向箭头；每次进入计划页时全部条目默认折叠。
- 周日程改为与日日程相同的屏幕宽度，七天按可用宽度平均分配，不再需要横向滑动。
- 快速记录加号从页面右下角移到底部“收集箱”和“计划”之间，避免遮挡正文和按钮。
- 日、周时间轴范围由 08:00–22:00 扩展为 06:00–24:00。
- 将“活动计时、分级预告与结束转场”加入 V2 路线图，并保存完整实现方案。

## 1.4

- 计划页改为纵向堆叠的可展开条目，首屏先显示课程、教程、目标、回顾和暂停项目的摘要。
- “课程与空档”内部按待确认课程、已确认课程、课程间空档依次堆叠，减少信息混杂并保留确认、忽略和编辑操作。
- 周日程色块移除内部文字并收窄；仍可点击色块查看完整名称、地点与精确时间。
- 周日程新增“显示课程信息”开关，按需在表格下方显示课程名称、地点和起止时间。
- 已完成的定时任务继续保留在日、周日程中并灰化；详情中显示完成状态且不再出现重复完成按钮。

## 1.3

- 今日页新增“日／周”日程切换入口；周视图按周一至周日横向排布。
- 日、周日程统一改为 08:00–22:00 连续时间轴，整点线固定显示，色块上下界对应真实起止时间。
- 课程按实际节次时间绘制；安排任务时可选择预计用时，任务色块高度按分钟计算。
- 点击任意课程或任务色块可查看类型、星期与精确起止时间；任务可从详情直接完成。
- 今日未指定时段的待办与固定日程分开显示，并增加当前时间红线，减少日程误读。
- 每周多次目标显示“已完成、已安排、还需安排”，每一次生成独立任务并可分别完成或改期。
- 增加无课程空档时的备用时段建议，让每周多次目标仍可逐次安排。
- Actions 改用固定的原型签名，并给 APK 与构建产物加入版本号；从 1.3 起后续版本可以直接覆盖安装。

## 1.2

- 弹性安排中的每项任务按实际类型显示对应标签和颜色，避免“弹性”文字与锻炼、休息等颜色不一致。
- 任务卡改为浅色底与窄色条，保留类型辨识度并减少大块灰绿色边框。
- 任务 ID 改为随机唯一值，修复完成一项时多个任务同时消失的问题。
- 自动迁移手机中已有的重复 ID；若多项因该问题被同时完成，仅保留第一项为已完成并恢复其余任务。

## 1.1

- 今日页改为始终可见的按小时日程表；没有事件时仍显示空档，而不再只显示空白表头。
- 重设事件配色与整体背景，课程、学习、锻炼和任务使用清晰但不过度刺眼的颜色。
- 收集箱每个项目提供“安排时间、编辑、删除”三个直接操作；安排后会转为日程任务。
- 新增目标弹窗可滚动，完整显示最低版本建议与创建按钮。
- 计划页顶端增加“从结果开始”入口，明确目标、最低版本和首次安排之间的关系。

## 1.0

- 主页面改为可横向查看的周日程表，课程、学习、任务、运动、娱乐与休息使用不同颜色。
- 所有主要页面支持纵向滚动，避免内容被遮挡。
- 收集箱项目可直接选择时间并加入日程。
- 新增目标先填写预期结果，应用提供可执行的最低版本建议。
- 已完成任务记录完成时间，并在今日页显示今日与本周完成统计。
- 地点功能收回到“设置 → 通勤与地点”，不再作为难以理解的独立页面。
- 修复周日程表的 Kotlin 编译错误。

后续每一次功能更新依次递增 0.1（1.1、1.2……）。
