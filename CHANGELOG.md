# FocusFlow 版本记录

## 6.4.0（开发中）

- 今日页新增“需要恢复的安排”：识别已经错过或至少改期两次的未完成任务，并提供缩小为 10 分钟短版、重新安排、放回收集箱三个明确动作。
- 本周回顾新增执行概览，展示计划完成率、改期数和待恢复数；同一时段至少发生两次改期时，才给出缩短任务或预留缓冲的低压力建议。
- 恢复建议完全基于本地现有任务字段，不自动移动、删除或改写任务；收集箱与暂停任务不会被误判为待恢复。
- 新增纯 Kotlin 恢复策略测试，覆盖错过、反复改期、忽略已完成／收集箱／暂停任务，以及周统计的数据不足规则。

## 6.3.0

- 日程任务详情补齐“开始／改期／完成”主操作，减少从时间轴返回今日页再寻找任务的绕路。
- 今日概览新增计划完成率、今日完成数与今日改期数；没有定时计划时不显示误导性的 0% 完成率。
- 今日完成任务以时间记录单独保留，同时已完成色块继续在日／周日程中灰色显示。
- 任务改期次数和最近改期时间写入本地存档；旧存档缺少字段时默认按零次处理，不删除或改写既有任务。
- 新增纯 Kotlin 统计测试，覆盖计划完成率、完成记录、改期与收集箱计数。

## 6.2.2

- 日程任务改为两段提醒：可选的提前预告不会再替代到点提醒，到达安排时间时会再次通知。
- 在提前窗口内临时安排或改期时，提前预告可立即出现，但到点闹钟仍会保留。
- 改期、完成、删除、重启或升级时同步清理并重建提前／到点两条闹钟，同时兼容清理 6.2.1 的旧单闹钟。
- 设置诊断分别显示下一次提前提醒和下一次到点提醒，避免把提前预告误认为唯一的到点通知。
- 后台诊断自动读取 Android 电池优化状态，并记录 1 分钟测试广播的预期／实际送达时间，区分按时、延迟和未按时送达；厂商自启动开关不可读取时不再假装已检测。
- 到点提醒与一分钟测试优先使用 Android 的强唤醒闹钟路径，提前预告仍使用普通精确提醒；强路径只唤醒并发布通知，不主动播放闹钟声音，但系统可能显示闹钟标识。
- 设置页将“活动结束前预告”和“日程开始前预告”分区命名；收集箱安排弹窗的模式与时长按钮改为两列换行，避免窄屏挤压“精确时间”和“90 分”。

## 6.2.1

- 修复从收集箱安排到具体时间的任务可能没有按时通知：Android 13+ 按日程核心用途声明精确闹钟权限；其他系统在允许时使用精确闹钟，不允许时明确降级为可能延迟的普通后台提醒。
- “日程与活动提醒”新增诊断卡：显示当前精确／普通调度模式、下一条已安排任务及提醒时间，并可安排 1 分钟测试通知。
- 通知权限或精确闹钟权限变化、设备重启及应用升级后，自动重排未来任务提醒。
- 精确提醒状态统一读取 Android 实际能力，不再同时显示“正在使用精确提醒”和“设备可能不支持”的矛盾文案。
- 修复正式 6.2.0 设置页仍显示 ColorOS 专用通知路径的问题，恢复按三星、小米、华为、OPPO、vivo、Pixel／原生 Android 等设备显示补充引导。

## 6.2.0

- 通知说明不再只针对 ColorOS：按三星、小米／Redmi／POCO、华为／荣耀、OPPO／realme／OnePlus、vivo／iQOO、Pixel／原生 Android 显示补充检查路径，其他设备使用通用 Android 路径；同时明确厂商私有横幅／悬浮开关无法可靠自动读取。

- 快速入门与通知帮助同步 6.2 主流程：先引导“快速记录 → 收集箱 → 日程 → 完成／推迟”，习惯、课表、目标、资料、AI 与地图改为按需配置；帮助不再声称可一键跳转 ColorOS 横幅开关。
- CI APK 文件名、artifact 名和设置页版本信息加入 Actions run 编号，避免误装同为 6.2.0 的旧候选包。
- ColorOS 横幅引导改为可验证流程：移除无法可靠跳转的日程／饭点渠道按钮，改为手动开启路径；应用每次回到前台检测系统公开的总通知与两个渠道状态，未开启时给出应用内提醒。
- 页面结构收敛：今日页按“现在 → 接下来 → 收集箱 → 条件模块”排列；未配置的饭点、目标、精力、睡前和校园模块不占空间。
- 计划与设置重叠：计划页使用堆叠入口并新增资料工具箱；地点、AI、课表识别、应用检测、稳定性和原始习惯数据统一进入设置的高级工具。
- 目标执行依据改为目标级：每个目标可单独编辑第一步行动、完成标准、资料与章节；安排后的任务携带该目标自己的执行依据。
- 教程降级：旧“当前标准”只保留为资料库常用标记，不再自动应用到新目标或批量覆盖目标。
- AI 保存更保守：搜索建议只可填为候选第一步；真实链接、材料或笔记经用户确认后才进入资料库，视频分析同样要求确认材料。
- 兼容 6.1 及更早目标 JSON；新增字段缺失时使用空值，不删除或改写旧目标、资料和完成记录。
- 渐进拆分日程、时间轴、计划主页、课程、空挡、目标、工具箱与纯策略逻辑，并补充对应单元测试。

## 6.1.0

- 饭点提醒闭环：划掉“准备吃饭”通知会记为当天不再提醒；过饭点 45 分钟后不补发“准备吃饭”；每天零点后自动续排下一天，不依赖再次启动应用。
- 正在用餐的结束提醒在日常重排、设备重启或应用更新后会恢复；不再被开始提醒的重排误取消。
- 日程提醒补全：定时任务和目标安排默认提前 10 分钟提醒，可在设置调整为 0–30 分钟；完成、删除、改期会同步取消或重建闹钟，设备重启/应用更新后恢复未来日程。
- 设置页改为“日程与活动提醒”：可重新申请通知权限，显示渠道可能被静音的状态，并可直达日程／饭点横幅的系统渠道设置。

## 6.0.0

- 发布版清理：移除内置示例课程（原 ScreenshotCoursePreview）与示例任务（拉伸/锻炼/睡前减速），新用户从空白状态开始，按快速入门逐步设置。
- 新人引导：首次启动依次 通知权限 → 习惯基线引导 → 基线提示 → 快速入门；快速入门含「习惯基线 → 课表 → 目标 → 空挡」上手步骤。
- 版本号升级到 6.0.0（正式发布；发布用 GitHub Release，不上应用商店）。

## 5.9.5

- 时间轴色块相切修正：去掉色块上下 1dp 内边距、保留统一 7dp 圆角，时间相连的色块直接贴合（圆角自然区分彼此）。
- 新增单元测试（纯逻辑对象）：CourseGapPlanner、GoalPlanner、GameStats、CheckInInsights、QuietHours、AppLibrary 共 6 个测试类 22 条断言，`gradle test` 全部通过。

## 5.9.4

- 日程时间轴色块圆角收窄（7→3dp），时间精确连续的色块更贴近相切。
- 自动排计划时不同活动之间预留 10 分钟缓冲，避免背靠背、留出切换余地。
- 长对话框（作息分组向导、新增目标、安排空闲活动、添加本机应用等）内容超高时右侧也显示滚动条。

## 5.9.3

- 课表截图识别（视觉模型）也接入顶部全局进度条。
- 主要页面（今日 / 日程 / 计划主页 / 计划子页 / 设置）右侧新增可视滚动条，提示下方还有内容。
- 「功能简介」改为「快速入门」：补充上手步骤（习惯基线 → 课表 → 目标 → 空挡）与核心功能说明（加号、深色、前台检测、崩溃记录等）。

## 5.9.2

- 全局进度条从底部移到顶部（输入时不遮挡、不被键盘挡住）。
- 视频分析：AI 分析门槛降到 10 字；「保存为新教程」不再要求先跑 AI——没简介/字幕也能填标题＋链接直接保存。
- 日程时间轴点击任务/活动新增「删除」：删除安排（含前台检测会话与提醒）。

## 5.9.1

- 安排空闲活动加"个性化频率提醒"：同一活动在计划当天安排次数超过你历史单日习惯时温和提示（基于你自己的记录，不写死规则；没有历史则不提示）。

## 5.9

- 深色模式修复：Material3 卡片默认用的 surfaceContainer* 色调此前未随深色调暗，导致"今天的目标"等卡片仍发亮；现已一并调暗。
- 全局进度条：导航栏上方在应用扫描 / AI 生成（学习路径、搜教程、视频分析、AI 总结）时显示细进度条。
- 前台应用检测：已分类 / 待分类 / 已忽略 三个分区各自折叠（默认已分类展开，其余收起）。
- 添加本机应用的分类 chips 可横滑（不再挤掉"其他"）。
- 空挡内容建议同日去重：同一目标/活动不会在同一天重复出现在多个空挡。

## 5.8.2

- 空挡建议地点提示细化：新增 游泳馆（游泳）、实验楼（实验）、研讨室/教室（讨论/小组/会议）、图书馆（图书馆/自习）等更具体的映射，更具体的关键词优先。

## 5.8.1

- 空挡内容建议按历史完成率排序：优先推荐该时段完成率较高的目标（样本 <3 不参与排序），完成率 ≥0.6 时在原因里标注"该时段完成率较高"。

## 5.8

- 本地崩溃上报：未捕获异常自动写入本机 crash.log（不引第三方、不上传）；设置 → 稳定性与崩溃 可查看、复制、清空崩溃栈，方便反馈定位。

## 5.7

- 空挡建议进阶：每个空挡的内容建议新增两个维度——①精力规律（按历史签到判断该时段通常"偏低/充足"，提示优先短任务或适合专注，数据不足不显示）；②地点提示（按目标/活动关键词提示 操场/体育馆、图书馆/教学楼、宿舍，仅提示、不假装精确）。

## 5.6.1

- 深色模式：设置 → 外观 开启，在当前主题基础上调暗背景/表面、调亮文字，保留主/副/强调色。
- 前台应用检测 → 应用分类：分类 chips 可左右滑动（“其他”不再被挤占）；每个应用增加「忽略」隐藏，被忽略的应用在「已忽略应用」里可一键恢复。

## 5.6

- 「想玩游戏」泛化为「安排空闲活动」：加号 → 安排空闲活动，可选类别（游戏/视频/学习/休息/运动/自定义）；游戏/视频到点检测前台应用收尾，其余活动到点提醒收尾；统计与周回顾改为「活动自律」。
- 空挡内容建议：空挡建议页每个 ≥30 分钟的空挡/自由时段给出「适合做什么」的内容建议（未完成目标优先，其次弹性任务），可一键排入。
- 作息分组向导：星期选择可左右滑动（不再需要先选 1-4 才能选 5-7）；每组需显式选择星期。
- 修复：应用分类扫描不全（Android 11+ 包可见性补 <queries> 声明，现在能列出本机全部启动器应用）。

## 5.5.2

- 修复：开启常驻快速记录闪退（前台服务补 foregroundServiceType=specialUse，适配 Android 14+）。
- 修复：系统"使用情况访问"列表不显示 FocusFlow（Manifest 补 PACKAGE_USAGE_STATS 声明）。
- 修复：在某作息方案里建分组后，切走再切回分组消失（编辑分组会同步回当前方案并保留「当前」标识）。
- 修复：作息分组向导每组默认全选剩余天数（下一组会被迫继承全部天数）——改为每组需显式选择星期。
- 修复：应用分类只显示约 10 个——改为列出本机全部已安装应用（按分类分组，未分类全量列出，可滚动归类）。

## 5.5.1

- 切回上学/考试周时校园生活自动重新开启（假期自动关，开学自动开）。
- 教程资料区按钮改两行排布，学习路径建议不再被视频分析挤压；折叠行标题与箭头同行（长标题省略号）。
- 视频分析不再要求开启“教程联网搜索”，填了硅基流动 key 即可用。
- 学习路径建议解析再加固：支持更多键名变体（标题/主题/关键词/搜索词…）、关键词缺失时用主题兜底，并增加一次带格式示例的严格重试，进一步减少“返回不是路径”。
- 切换作息方案：作息分组严格随方案切换（不再沿用上一方案残留分组）；已切换的方案显示「当前／已切换」标识，编辑作息后标识自动清除。
- 应用分类改为按本机已安装应用生成：内置常见应用归类 + 应用名自动识别，未分类应用按最近使用列出供归类。
- 前台应用检测的说明移入帮助（小问号），设置页保留简洁说明。
- 权限：开启“前台应用检测”时自动跳转系统授权“使用情况访问”；从系统设置返回后状态自动刷新。

## 5.5

- 假期阶段自动关闭校园生活（切回上学/考试周后由用户手动再开）。
- 教程资料折叠到一起：目标与执行页教程区收为一行（已收集 N 项 · 当前标准），点击展开全部内容。
- 视频分析一站式整理：教程资料区新增「＋ 视频分析」——粘贴视频标题/链接/字幕 → AI 生成要点＋适用目标 → 直接保存为新教程；模型入口在 设置 → 学习路径建议（免费/推荐/自定义，模式同前）。
- 想玩游戏：时长快捷项去掉 120 分钟（保留 30/60/90 + 自定义输入）；建议时间新增「自定义时间…」（时间选择器，取最近一次该时刻）；「到点提醒开始」改为可选（默认关闭，结束提醒始终有）。
- 切换作息方案时作息分组随方案切换；旧方案无分组时保留当前分组，避免误清空。
- 应用清单改进：显示应用名（未安装的内置项只显示包名）；新增「＋ 添加本机应用」——搜索本机已安装应用并归类（自动识别提示，可改）。
- 周回顾新增「AI 周总结」：按本周真实记录（目标完成、常见阻碍、游戏自律）生成简短复盘；每个目标的调整建议仍在各自卡片下方（数据式）。

## 5.4

- 前台应用检测与游戏自律（加号 → 「想玩游戏」触发，与原有入口不冲突）：
  - 应用分类：内置清单（常见游戏/视频/社交）+ 按应用名自动识别 + 手动归类；设置页「前台应用检测」可查看/修改分类、给最近未分类应用归类。
  - 安排游戏时间：按本周空闲时段建议开始时间与时长；到点自动记录状态并提醒开始；到点检测前台应用——还在玩游戏类应用就提醒收尾（可结束/再玩 15 分钟，10 分钟后复查），不在玩则自动记录按时结束。
  - 统计与周回顾：记录每次游戏安排的实际结束与超时；周回顾新增「游戏自律」卡（本周安排/按时/超时统计 + 数据式建议）。
  - 权限：使用情况访问（系统设置一次性授权，分钟级精度足够）；未授权时只提醒开始/结束，不检测前台。
- 说明：前台应用判断只在本机完成，不上传任何数据。

## 5.3

- 新建目标时「搜学习教程」（替换原“生成学习路径”按钮）：手动搜索（B站/知乎/慕课 一键跳转，关键词预填目标名＋预期结果）；AI 生成 3–5 条“去哪个平台搜什么”的建议，每条可直接「去搜」或「保存为教程」——保存后自动设为当前标准并回到新建目标（已填内容保留，目标自动带教程依据）。
- AI 建议模型沿用教程联网搜索的设置（免费默认 / DeepSeek-V4-Flash / 自定义），未开启时手动搜索仍可用。

## 5.2.1

- 今日页餐点卡片移到收集箱上方（统计 → 餐点 → 收集箱）。
- 「当前标准」横幅的具体内容改为可展开（默认只显示标题行，点击展开说明）。

## 5.2

- 教程标准可感知：教程资料区新增「当前标准」横幅（未设置时提示后果）；没有依据的目标显示「未设教程依据」；设为标准/取消标准即时提示；新增「全部应用当前标准」一键把当前标准关联到所有未设依据的目标（不覆盖已有依据，目标卡随即显示“依据”并可打开教程）。
- 「现在做什么」回归活动记录：记录时只问正在做什么（不再混问精力）；选择娱乐类可顺手设置收尾提醒（30/60/90 分钟，可跳过），到点提醒结束、可处理到点或延长；精力改为今日页独立「当前精力」卡（只影响弹性任务推荐顺序）。
- 每日定时「状态询问」（精力＋活动，用于时段规律学习）保持不变。

## 5.1

- 修复：日程里已有安排（排好的目标任务/事项）不再被当成空闲——课间空挡与「自由时段」的可用时长会扣除这些占用段（自由时段按占用切分成剩余子段显示，如“15:30–18:00”“19:30–22:00”）；目标建议、自动排本周目标、新增目标时的空挡统计均避开已占时段。
- 自动排本周目标时，同一次自动排内后排的目标也会避让先前排入的任务，避免多个目标重复占用同一时段。

## 5.0

- 安排不只课间空挡：新增「自由时段」（最后一节课后到晚上的整块空闲、没有课的整天），空挡建议页单独列出，自动排本周目标与目标建议一并纳入。
- 作息分组改为向导式：设定作息 → 选中要应用的星期 → 确定这一组；未分配的星期继续下一组；全部覆盖后按星期自动命名保存（如“周一至周四”“周五”“周六、周日”）。
- AI 总结防退化：改进提示词（逐条“要点N：”、保留具体细节、禁止重复/无意义字符）；新增退化检测（输出过短或单字符占比过高，如大量“1”）并自动用更严格提示重试一次，仍异常则明确提示。
- 教程「设为标准」作用可见：目标卡“依据：《教程》”旁新增「打开教程」按钮（有链接时直接打开）；教程资料区补充说明“设为标准”的效果（新建目标自动作为依据、目标任务详情带教程提示）。

## 4.9.1

- 修复：学习路径建议首次输出不符合要求时的重试逻辑——不再复用已读取响应的同一连接（原会抛 ProtocolException “cannot write request body after response has been read”），改为每次请求新建连接。

## 4.9

- AI 总结：教程资料每项新增「AI 总结」——粘贴教程/文章正文，生成 3–5 条中文要点与适用目标，保存为该教程的“总结”字段（目标卡/教程列表展示）；内容只发往硅基流动，不保存原文，模型沿用学习路径建议的设置（免费默认 / DeepSeek-V4-Flash / 自定义）。

## 4.8

- 生活基线按天/多段作息：习惯基线新增「作息分组（按星期）」——为任意星期组合（如周一至周四／周五／周末）设置独立的起床、睡觉、餐次锚点，周五课少≈周末、周末晚起等场景按天生效。
- 生效点：饭点学习/今日餐点按当天所属分组取锚点；睡前减速（今日页提示 + 每晚提醒）按当天分组睡觉时间；提醒调度按分组重排。

## 4.7

- 目标智能化（一期，数据式）：
  - 新增目标对话框显示「本周课程空挡可安排约 N 次（共 M 段空挡，中位单次约 X 分钟）」的数据式建议（课程空挡统计，数据不足不显示）。
  - 新增「生成学习路径」按钮：以目标名为标题打开学习路径建议（复用 4.6 功能）。
  - 完成率学习：按（星期 × 开始小时）记录自动排入目标任务的排入/完成次数（完成来自完成确认对话框与通知完成按钮）；自动排目标时优先历史完成率高的时段（未知时段排最后），样本 <3 不参与。
- 学习路径解析再加固：说明文字与 JSON 混杂时逐对象提取；首次输出不符合要求时自动重试一次（追加“只输出 JSON 数组”指令），降低“返回不是路径”。

## 4.6.3

- 学习路径建议完善：每一步新增「去 B站搜」按钮（一键跳转 B 站搜索该关键词），资源类型改为纯标签（不再是无响应的可点 chip）。
- 提示词强制 3–5 步（数组至少 3 个元素），输出上限 800→1200 tokens，减少“只有一个结果”。
- 解析容错：兼容模型返回单个对象而非数组、键名变体（title/搜索关键词），减少“返回内容不是路径”。

## 4.6.2

- 内置默认地点可删除：管理校园地点里，内置/地点包地点现在也有「删除」（隐藏）按钮，隐藏后不再出现在教学楼选择、通勤与空挡计算中；新增「已隐藏的默认地点（可恢复）」区，一键恢复。

## 4.6.1

- 修复：设置页“常驻快速记录通知”与“状态询问”之间重复的分割线。
- 修复：所有子页面（课程/空挡/目标/回顾/暂停/设置子页）内容容器补上垂直间距，圆角矩形不再相切（此前只在个别 chips 加了间距，未覆盖子页）。
- 教程搜索重定位为「学习路径建议」：不再让模型编造候选教程链接（原结果是模型臆想、链接不可靠），改为生成 3–5 步可执行路径（每步：学什么/资源类型/去 B站、知乎、慕课 搜什么关键词/为什么），用户按关键词在真实平台搜索，搜到的有用内容手动收藏；设置页与目标页入口同步更名。

## 4.6

- 习惯基线：已存方案折叠为「已存方案（N）· 展开 ▾」，默认收起，更简洁。
- 通勤与地点页模块化：出行参数、校区中心与手动位置分别收入标题卡片（模块标题加粗、子项标题加粗），显示更有条理。
- 间距调整：课程编辑的教学楼/星期 chips、模型预设 chips 等相邻圆角矩形拉开间距，避免相切（日程表除外）。
- 教程搜索模型预设：设置页新增 免费（Qwen2.5-7B，默认）／DeepSeek-V4-Flash 一键切换，也可手填；与课表识别的模型预设模式统一。

## 4.5

- 地点库从地图初次配置：管理校园地点 → 高德 key 已填时新增「从地图批量提取」——按教学楼／图书馆／体育馆／操场／食堂／宿舍／实验等关键词在校园 3 公里范围批量提取候选地点（去重、跳过已存在），默认全保留，可逐项或一键「全部保留／全部忽略」，确认后一键加入地点库（自动按名称前缀猜分区、按 POI 类型猜用途，保留坐标）；地点库从此不依赖新增课程。

## 4.4.4

- 教学楼地点库独立于课程：新增/编辑课程时用“其他”自填的楼名会自动进入校园地点库（按东/西/北猜分区），之后成为教学楼选项并参与空挡/路程估算；地点库也可直接在 设置 → 通勤与地点 → 管理校园地点 单独维护（添加地点不再依赖新增课程）。

## 4.4.3

- 今日页顺序微调：完成统计（今日完成/本周完成/待整理）上移到收集箱之前——紧贴行动卡提供即时反馈，“待整理 N 项”引出收集箱；今日餐点随之下移。最终顺序：阶段 → 今天接下来 → 现在做什么 → 今天的目标 → 完成统计 → 收集箱 → 今日餐点 → 今日活动记录 → 睡前减速 → 校园生活开关。

## 4.4.2

- 修复：今日页“今日餐点”重复显示（4.3 重构时反馈层插了两遍，已删除重复的餐点/统计/睡前块）。
- 新增课程/编辑课程：教学楼新增「其他」选项并支持自填楼名（按东/西/北自动猜分区），不再局限于已有地点目录。

## 4.4.1

- 空挡课表视图改为与日程一致的周时间轴课表（复用课表色块样式）：课程块同课表，课程间隙标注扣除路程后的净可用分钟数（≥60 分钟以深色标签突出，适合安排目标或充电）。
- 入口改为折叠卡片：计划 → 空挡建议 顶部「空挡课表视图」卡片，点按展开/收起，不再跳副页面。

## 4.4

- 空挡表格视图：计划 → 空挡建议 新增「表格视图」入口，周 × 节次网格可视化——课程块显示名称，课程间隙显示扣除路程后的净可用分钟数（≥60 分高亮，适合安排目标或充电）；返回键回空挡列表。
- 空挡卡片升级：显示具体钟点（如“周一 10:15–11:45”）、总时长、路程、净可用时长，不再需要脑补换算。

## 4.3.2

- 假期阶段不再把课程当作日程：生活阶段切到“假期”后，日程、今日“今天接下来”、空挡建议与目标建议都不再显示/计算课程（课程管理页仍完整保留，切回上学/考试周自动恢复）；自动排计划同样按假期无课程处理。

## 4.3.1

- 首次启动功能简介：权限申请与习惯基线引导结束后自动弹出“欢迎使用 FocusFlow”（四个入口 + 数据原则，只弹一次）；之后可在 设置 → 功能简介 再次查看。
- 首次完成习惯基线后弹出“后续在哪找”提示（只弹一次）：指向 设置 → 习惯基线（编辑/另存方案/生活模式）与今日页阶段切换。

## 4.3

- 今日页行动化：首页按“状态 → 安排 → 行动 → 目标 → 捕获 → 反馈”重排。
- 生活阶段一键切换置顶：假期／上学／考试周 chip 行（原埋在设置→习惯基线），切换即生效并重排饭点/睡前提醒。
- 新增“今天接下来”摘要卡：课程+任务按时间合并显示接下来 2–3 项（当前在上的课高亮），点卡进日程。
- 精力与“下一件合适的事”合并为“现在做什么”行动卡：精力 chips + 推荐行动（开始/最低版本/自由开始/改时间/完成）+ 时段精力建议；进行中活动时自动变为会话卡（倒计时/到点转场）。
- 新增“今天的目标”卡：今天已排目标任务带教程/最低版本指引可直接开始；未安排时引导去“按空挡自动排本周目标”。
- 校园生活开关移至页尾小行，收集箱上移到反馈层之前。

## 4.2

- 教程标准可管理：教程资料可删除、取消“当前标准”、重选其他教程；教程联网搜索的候选保存后自动设为“当前标准”，新建目标时自动作为教程依据。
- 教程执行生效：手动安排或“按空挡自动排本周目标”生成的目标任务，详情自动带上教程与最低版本指引（“教程：《概率论》（第 1 章）· 最低版本：先投入 15 分钟”），执行时直接照着做，解决“收藏了教程但没实际效果”。
- 识别表格＋本地判断自动排计划：目标与执行页新增“按空挡自动排本周目标”——把本周未完成的目标次数排进课程空挡（本地判断：避开课程与已有安排、优先更长空档），一键生成带提醒的任务并进入日程，可随时改期；无可用空档时给出明确原因。
- 帮助文档补充教程功能说明（目标与执行节）。

## 4.1

- 生活模式多方案：同一生活阶段（假期/上学/考试周）下可把当前作息“另存为方案”（命名，最多 8 套），在“习惯基线”里一键切换或删除；饭点学习与睡前减速按当前方案的阶段自动跟随。
- 提醒打扰控制：免打扰时段（起止可跨天，如 23:00–07:00）静音状态询问、饭点提醒与睡前减速（每类可单独开关），活动到点和任务提醒保持时间敏感不静音；一次性静音 1 小时／3 小时／到明早 7 点，静音结束自动恢复。
- 常驻快速记录通知：设置里开启后通知栏常驻一条静音通知，点“快速记录”直接打开收集箱；重启后自动恢复。
- 询问时刻自动决策：签到数据充分（≥3 次）且未手动调整时，自动采纳建议询问时刻并重排提醒，设置页显示“已自动调整（手动调整后不再自动）”。
- 校园模式首页开关：今日页顶部可直接开关校园生活（不影响课程与日程）。
- 电动车联动：电量“偏低”时，计划→空挡建议页给出本周可用的充电空档（≥60 分钟），设置页同步提示避开远距离连续行程。
- 睡眠／娱乐习惯自动识别：结合签到与活动记录，深夜（22 点后）仍活跃 ≥3 次时，睡前减速提示与通知给出“建议更早收尾”的警示；当前时段恰逢常见娱乐时段（样本 ≥3 次）时提醒留出收尾时间。
- 决策：正式理财（v4）评估后不做——与“本地日程与执行辅助”定位不符，消费草稿保持现状，从路线图移除。

## 4.0.2

- key 申请引导：首次开启「课表识别（视觉模型）」且未填 key 时自动弹出“如何获取硅基流动 API key”三步引导（只弹一次），可一键跳转硅基流动 API 密钥页；设置页与帮助（小问号）里也有「如何获取 key」入口。免费模型（Qwen3-VL-8B）注册无需充值，降低新用户使用门槛。

## 4.0.1

- 移除本地 OCR：本地 OCR（ML Kit / Tesseract）识别效果差，已整体移除——课表导入只使用硅基流动视觉模型，识别失败直接提示原因（检查 key、模型名或网络后重试），不再产生低质量兜底结果；移除 OCR 依赖与 tessdata 资源，APK 体积明显减小。
- 导入前置引导：未开启视觉模型或未填写 key 时，点“选择课表截图”先提示去设置开启，不再白选图。
- 楼级归并补全：无房间号的楼座（“东1B”“东一B”“东1B栋”）与带房间号（“东1B-201”）统一归并为“东1教学楼”；容忍模型输出末尾省略号（如“东1B-2...”）。

## 4.0

- 课表识别（视觉模型，可选）：设置里开启后，导入课表截图改用硅基流动视觉模型识别，识别失败自动回退本机识别；与教程搜索共用同一把硅基流动 key，课表图片仅发往 api.siliconflow.cn，关闭开关后导入课表不联网。
- 模型预设：设置页提供 Qwen3-VL-8B（免费）/ 32B / 30B-A3B / PaddleOCR-VL（OCR 专用）一键切换，也可手填其他模型 ID；旧 Qwen2.5-VL 系列已下线，保存过的旧模型名启动时自动迁移到对应 Qwen3-VL 新 ID；识别输出上限提高到 4096 tokens，大课表不再因截断报“没有解析出课程”。
- 课名/地点分开与页脚过滤：识别严格要求课名只填课程名称、location 只填教室或楼名；忽略课表网格外的说明文字（页脚、“隐藏课程信息”等），不再把非课程文字当成课。
- 地点归到楼级：教室号自动归一为楼名（如“紫金港东1A-302”→“东1教学楼”），自动剥校区前缀（含通用“××校区”规则，适配其他校园），中文数字转阿拉伯数字，命名与地点目录/地图保持一致；找教室靠“教学楼进出与找教室缓冲”时间。
- 新地点待用：识别结果中不在已有地点目录的楼名保留下来，自动进入 设置→通勤与地点→“课表识别发现的新地点”，可一键加入地点目录（按东/西/北/化学自动猜测分区），供空挡与路程估算使用。
- 冲突课程视觉提示：待确认列表里与已确认课程时间冲突的课程卡片加警示边框与“⚠ 与已确认课程《xxx》时间冲突”标注，确认后日程页有斜纹警示兜底。
- 待确认课程批量操作与冲突保留：待确认区标题行右侧新增“全部忽略”一键清空；识别结果与已确认课程时间冲突时不再拦截丢弃，保留为待确认并提示“请核对后再确认”，由用户决定确认/编辑/忽略（已有冲突警示机制兜底）。

## 3.9.18

- 自定义主题防撞色：文字色与背景色对比度不足 3:1 时禁止选用（色板中标斜杠），自定义取色时实时显示对比度并拦截低对比度应用；文字色只可与实际背景对比、中性色（背景）只可与文字色对比。
- 颜色选择升级为调色盘：预设色板补上黑白灰阶（纯黑、纯白可选），新增色相滑杆＋饱和度/明度取色区＋#RRGGBB 十六进制输入。
- 外观页内置主题卡按钮重排：“改色另存”改为单个“以此改色”文字按钮，选中状态“已选择”并入标题行，不再上下叠放；“以此改色”只带入配色不立即切换主题，在自定义页确认后点“应用此配色”才启用（避免界面提前变色造成违和），预设“应用”同样直接启用自定义主题。

## 3.9.17

- 预设可编辑：点预设上的编辑按钮加载该配色修改，改完点“更新此预设”保存到原预设；应用、删除、另存时自动退出编辑状态。
- 内置主题“改色另存”：外观页每个内置主题卡新增按钮，以该主题的全局色为基础进入自定义编辑器，调整后保存为新预设；原生内置主题本身不被改动。

## 3.9.16

- 设置页副页面在底栏有反应：进入外观、自定义主题等副页面时，底栏"设置"图标变为"◉"并显示当前副页面名（"设置·外观"等），与计划页副页面逻辑一致。

## 3.9.15

- 自定义主题支持保存预设：调好的配色可“保存为预设”并命名（最多 8 套），列表一键应用、可删除；当前配色与某预设一致时显示“当前配色”。

## 3.9.14

- 砍掉 3/4 色档位，只保留一套全局主题：五个全局色（主色/副色/强调色/中性色/文字色）共同影响除课程色块与提醒警示外的所有界面区域，外观页与自定义主题页的档位切换随之移除。
- 自定义主题页固定显示五个全局槽位，课程色块与提醒警示保持各自语义、不接受定制。

## 3.9.13

- 5 色档位改为全局配色：主色/副色/强调色/中性色/文字色，共同影响除课程色块与提醒警示外的所有界面区域（背景、卡片、按钮、正文、提示文字全跟随）；不再只是日程色块。
- 外观页帮助问号也移到标题行右侧，与自定义主题页一致。

## 3.9.12

- 自定义主题页的帮助问号移到标题行右侧，不再单独悬在内容上方。

## 3.9.11

- 配色档位三档各自独立、互不覆盖：3 色档沿用原来的主色/副色/课程色分工（副色槽位回归，可自定义）；4 色档按功能语义分工（主操作/强调/日程/中性）；5 色档改为日程配色，课程/学习/运动/娱乐/休息五种日程色块各自独立换色。每档都有其他档位调不到的槽位，切换档位即换一套分工。
- 提醒与警示（活动到点、睡前减速"注意休息"）固定使用系统提醒红，醒目一致，不随配色变化。

## 3.9.10

- 自定义主题槽位改为按功能语义分工：主操作色（按钮、开关）、强调色（提示文字）、日程色（日程色块）、中性色（背景与卡片）、警示色（提醒警示）；配色档位相应调整为 3 色常用 · 4 色加背景 · 5 色加警示。

## 3.9.9

- 权限申请策略：通知权限改为首次启动时一站式申请（带回调，申请后自动跟进精确闹钟）；申请过一次不再重复打扰。习惯基线引导在权限弹窗期间不会叠加。
- 活动提醒页新增权限状态行：通知未开启时红字提示并可一键跳转系统设置，从系统设置返回自动刷新。
- 精确闹钟按设备支持情况处理：ColorOS（OPPO/realme/OnePlus）实测授权页无开关，自动放弃并改用普通后台提醒。
- 主题多色档位：配色档位可选 3 色常用（主操作/强调/日程）、4 色加背景、5 色加警示，外观预览与自定义槽位同步。
- 自定义主题：槽位按功能语义分工（主操作色管按钮开关、强调色管提示文字、日程色管日程色块、中性色管背景卡片、警示色管提醒警示），从 30 色预设色板点选，立即全局生效并记住；可一键恢复最近用过的内置主题。
- 四套内置主题均新增强调色（tertiary），用于今日页精力建议与"下一件合适的事"提示文字。

## 3.9.8

- 提醒通知全部升级为横幅弹出（像微信一样弹出几秒＋声音）：活动结束、结束预告、任务、状态询问、饭点、睡前减速均为高重要性渠道；温和版到点提醒为静音横幅（仍比普通提醒醒目）；旧低重要性渠道自动删除。

## 3.9.7

- 设置页枢纽化续：外观（主题选择）与活动提醒（开关、提前预告与延长上限）同样改为枢纽卡片＋副页面，与计划页一致；设置主页只保留短分区与枢纽卡片。

## 3.9.6

- 设置页枢纽化：通勤与地点、教程联网搜索两块由页内展开收起改为与计划页一致的枢纽卡片（标题＋摘要＋箭头），内容移入对应副页面，副页面内保留问号帮助；设置主页大幅缩短。

## 3.9.5

- 设置页与版本路线图箭头统一换为 Material 矢量图标，修复文字与箭头的垂直对齐问题。
- 教程联网搜索默认模型改为 Qwen/Qwen2.5-7B-Instruct（旧默认已从硅基流动下线）；已保存的旧默认值自动迁移，模型类错误提示附更换引导。

## 3.9.4

- 设置页折叠分区样式统一：去掉独立卡片背景，与页面其他分区一致的扁平标题行；问号紧贴标题、摘要与统一箭头“›”（与版本路线图同款，展开时旋转 90°）置于右侧，消除色差与突兀感。

## 3.9.3

- 设置页折叠分区卡片重做：通勤与地点、教程联网搜索整块收进圆角卡片，点击头部行带涟漪反馈与旋转箭头，内容平滑展开收起，收起时只占一行。

## 3.9.2

- 移除后台通勤记录功能：评估后认为出行方式固定假设、时段不区分导致路线学习数据质量差，投入产出比低；保留手动"记录本次耗时"路线学习。应用不再申请定位权限，也不包含前台定位服务。
- 设置页优化：通勤与地点、教程联网搜索两块默认收起（标题行显示状态摘要）；从子页面返回或切换标签后保留原滚动位置。
- 校园地点分区新增"其他"，自定义地点可归入；与其他分区之间用默认 12 分钟兜底估计，可手动校准、可被路线学习覆盖。
- 状态询问时刻的"数据充分时自动采纳建议"列入后续候选。
- POI 搜索准确度提升：改为以校区中心 3 公里为范围的周边搜索（按距离排序，结果标注距校园中心米数），空结果才降级全城搜索；全城文本搜索增加 citylimit 锁定城市。
- 校区中心可配置：设置页可改 POI 搜索的圆心（经纬度）与城市（默认浙大紫金港 · 杭州），其他学校可改为自己学校后使用搜索。

## 3.9

# FocusFlow 版本记录

## 3.9

- 校园地点三态：设置页新增“管理校园地点”，内置紫金港目录之外可本地增删改自定义地点（可填坐标）、可选填写高德 Web 服务 key 搜索校园 POI 一键加入；外部地点包（JSON）继续作为 DLC 覆盖内置目录。
- 后台通勤记录（可选）：设置页开启后，手动选择起点/终点开始记录；前台服务内用系统定位低频采样（约 60 秒/100 米，零 SDK 依赖），持续通知可见、可随时结束；未授权或定位不可用时自动降级为纯计时。结束确认后写入路线学习。
- 教程联网搜索（可选）：计划页“教程资料”可联网搜集候选教程并比较来源；复用设置页填写的硅基流动 key（仅存本机、仅发往 api.siliconflow.cn），无 key 时功能禁用。

# FocusFlow 版本记录

## 3.8.4

- 睡前减速卡新增“明日准备”：进入减速阶段后按明日具体安排生成摘要——明日课程数及最早一节课的名称与时间（无课则注明）、明日已安排任务标题（最多 2 项）、收集箱待整理项数。
- 日程表冲突课程色块移除边框，改为红色块内的斜向条纹警示；与相邻安排块按真实分钟数紧贴排布、无视觉间隔，以四角圆角区分前后两个安排。


## 3.8.2

- 日程表中冲突课程改为单一警示色块：同一时间段重叠的课程合并为一个覆盖整个冲突区间的色块（黄框红底白字），不再分成并排小色块，标题与详情列出各门冲突课程。


## 3.8.1

- 课程冲突显示统一：日程表中冲突课程改为黄底深红边框的统一警示色块（深浅主题均清晰），不再只是红框；计划页课程列表单独列出冲突课程并标注与哪门课重叠，可直接编辑修正；计划主页课程入口显示“⚠ N 门冲突”。
- 空档建议区分碎片时间：不足 10 分钟的空档单独列为碎片时间区块（仅够通行与缓冲，不参与安排建议），10 分钟以上仍显示在可安排空档中。


## 3.8

- 今日页精力卡片新增按时段（上午／下午／晚上）的精力规律与常见活动汇总，并结合当前时段给出建议；签到不足 3 次或时段样本不足时不出具体建议，不假装精确。
- 设置页状态询问区块新增建议询问时刻：按签到时间分布给出每日询问时间建议，可一键采纳。
- 计划页本周回顾升级为每周目标回顾：每目标显示本周进度条、近 4 周完成趋势（历史来自完成反馈记录）与低压力调整建议；页面顶部汇总本周累计完成。
- 睡前减速：每晚按习惯基线填写的睡觉时间提前 40 分钟提醒开始收尾，可在设置页关闭；今日页睡前 4 小时起显示减速提示，并按明天第 1–2 节是否有课给出“注意休息”或“可稍晚收尾”；过点熬夜时只给低压力建议。
- 本周回顾页新增长期反馈分析：积累 5 条完成反馈后汇总最常见阻碍、难度分布与最低版本占比，并结合最近两周变化给出心态与安排调整建议。
- 设置页新增个人账目区块：统计填写的消费草稿金额合计、本月支出、分类金额、常去地点与餐次分布；只统计手填金额，不自动记账或推断。


## 3.6

- 课表识别不再依赖 Google Play 服务：无 GMS 的设备自动改用随 APK 打包的本地识别模型，课程名称、星期与节次照常解析为待确认课程；有 Google Play 服务的设备仍使用识别质量更高的在线模型。
- 课表导入新增防冲突：识别结果与已确认课程时间冲突时拦截导入并列出冲突明细；识别结果内部互相重叠也会提示核对；确认或忽略课程后导入提示自动清空。
- 日程时间轴上时间重叠的课程块以红色警示边框标出，便于发现冲突课程。

## 3.5

- 设置页新增“版本路线图”副页面：显示当前版本、已实现版本演进（1.0 → 3.4）、3.5 计划与后续候选；不再提供候选勾选，改为纯展示。
- 全应用新增帮助问号：今日、日程、计划、设置页的功能说明收敛到标题右侧的圆形“?”入口；设置页每个功能区块标题右侧各有一个问号，正文保留操作引导。
- 版本演进数据来源于本文件，发版时需同步更新 RoadmapData.kt；CI 构建产物文件名改为自动读取 versionName。

## 3.4

- 结束用餐时补齐消费草稿字段：地点（可快捷选择宿舍／食堂／外卖／便利店，也可自由填写）、餐饮分类（食堂／外卖／自己做饭／便利店／其他）、商家或食物名、支付方式（微信／支付宝／校园卡／现金／其他），与原有金额、评价、备注一起保存。
- 今日餐点卡片显示每餐“常去／上次在”的地点提示，来自最近一次确认地点；就餐记录列表完整展示地点、分类、商家、支付方式等草稿字段，仍可随时删除。
- 所有草稿字段只保存在本机、不自动生成账目，为后续版本的个人账目分析保留原始数据。
- 3.4 之后的地图与后台定位仍按计划在后续版本加入，需要地图 SDK 密钥并在真机验证。

## 3.3

- 今日页新增“今日餐点”卡片：按当前生活阶段与星期显示每餐的预计时间，临近预测点时提示“准备吃饭？”，可随时提前记录“现在吃”。
- 饭点学习只使用你确认的开始与结束时间，按“生活阶段 × 星期 × 餐次”分组取最近 8 次的中位数；不足 3 次时只用你填写的基线，并明确标注“数据不足”，不假装精确预测。
- 提醒流程：预测点附近通知“准备吃饭？”（已在吃／稍后 20 分钟／今天不需要）；确认“已在吃”后按个人时长估计结束时间，到点通知“吃完了吗？”（吃完了／还在吃／不记录）。
- 设置页新增“饭点学习”区块：饭点提醒总开关、每餐当前学习到的规律与样本数、就餐记录查看与删除入口。
- 结束用餐时可选择记录消费草稿（金额、评价、备注），为 v4 理财预留字段；不记录不会被视为没吃，也不会写入学习数据。
- 就餐记录与消费草稿均只保存在本机；重建习惯基线不会清除就餐记录，原始数据始终可查看、修正或删除。

## 3.2

- 首次打开会进行约 2–3 分钟、可随时跳过的习惯基线引导：选择当前生活阶段（假期／上学／考试周）、大致起床与睡觉时间、各餐开始时间与时长，以及常见娱乐时段。
- 原始事件按时间追加保存且不因学习而覆盖：活动开始／结束／跳过、任务安排／改期／完成、精力签到、通勤耗时确认、作息与餐次锚点、生活阶段切换都会留下记录。
- 设置页新增“习惯基线”区块：随时查看当前基线、回到引导修正、浏览原始事件记录，或一键重建基线（会清空全部原始事件）。
- 假期与上学节奏分开保存，后续饭点与作息学习按“生活阶段 × 星期 × 餐次”进行；数据不足时只给出宽松提醒。
- 本轮只保存经用户确认的数据结构与事件流，不进行任何自动推断；系统推测的内容会明确标记为待确认，不写入学习数据。

## 3.1

- 校园路线不再被单次“校正值”永久覆盖；每次实际耗时作为一条用户确认记录保存。
- 同一出行方式与分区组合最多保留最近 12 次记录，并使用中位数更新课程空挡、目标候选时间与路线预览。
- 路线预览显示确认次数和最近五次耗时，便于识别异常数据。
- 新增撤销最近记录和清除该路线学习；清除后立即恢复内置保守估计。
- 自动迁移旧版单次校正值，将其作为第一条确认记录继续使用。
- 3.2 将在地图坐标可用后加入可选后台定位辅助记录；默认关闭、持续通知可见，并可随时停止。

## 3.0

- “开始活动”新增可解释的建议时间，并显示采用建议后的预计结束时刻。
- 建议依次参考最近同名活动、同类活动、任务原有预计时长和类别保守默认值；历史用时采用中位数，降低偶发超长或过短记录的影响。
- 当前精力偏低时会建议更容易开始的短版；充足或正常时不会擅自延长用户原定任务。
- 若下一项课程或固定任务临近，建议会保留 15 分钟缓冲；剩余时间不足时明确提示先准备下一项或只做五分钟短版。
- 建议仅提供“采用建议时间”按钮，用户仍可自由填写时长或结束时刻，不会自动开始活动。
- 将 3.1 规划为基于用户确认记录的通勤时间学习，不使用静默定位推断。

## 2.9

- 设置中新增可关闭的“每日低打扰询问”，可选择 08:00–22:00 的询问时刻和 30／60／120 分钟的主动推迟时长。
- 状态询问同时记录精力与当前活动；今日页显示最近记录，并保留随时手动签到的入口。
- 通知可直接打开签到或选择稍后；没有回应时当天不连续追问，已有活动进行中则自动退避到活动结束以后。
- 签到记录仅保存在本机，当前只更新弹性推荐所用的精力状态，不会自动移动固定日程。
- 为 V3 保留最多 365 条经用户确认的签到历史，后续学习时段精力和常见活动时不使用未确认推断。

## 2.8

- 收集箱“安排时间”升级为推荐空档、大致时间和精确时间三种方式。
- 推荐空档复用现有排程能力，避开已确认课程和未完成的定时任务，并保留 15 分钟缓冲。
- 大致时间支持今天下午、今天晚上、明天上午、明天下午和本周内等范围，不伪装成精确时刻。
- 弹性时间范围作为独立字段保存在本机；后续初步安排只会在该范围内寻找候选。
- 日程页会显示弹性范围，并可重新调整范围或确认具体候选；只有确认具体时间后才创建提醒。
- 开发计划加入 2.9 低打扰状态签到，以及 V3 基于用户确认记录的时段精力与活动学习。

## 2.7

- “下一件合适的事”从只读建议升级为可执行入口，可直接开始、改时间或确认完成。
- 目标任务显示“最低版本”入口，按目标预计时长生成 5–15 分钟的低压力活动预填。
- 当当前精力偏低，或近期反馈集中在“时间不够／精力不足”时，推荐理由会明确提示最低版本。
- 从推荐开始活动会预填名称、类别、预计时长和下一项固定安排；开始前仍可修改。
- 开始活动不会自动把任务标记完成，活动结束后仍由用户明确确认，避免误记完成情况。

## 2.6

- 课程副页面新增课表截图导入，使用设备端中文文字识别，不上传课表图片。
- 识别结果只生成“待确认课程”，不会直接写入周日程或参与空挡计算。
- 结合截图中的星期列、节次行、课程名称与教学楼生成草稿，并过滤已有课程，避免重复导入。
- 待确认课程新增“编辑并确认”，方便修正识别出的名称、节次和教学楼；原有手动新增继续保留。
- 对不含课程文字、星期或节次的截图给出明确提示，不以猜测结果替代确认。

## 2.5

- 日程页的弹性任务新增“初步安排”，在未来七天内生成最多三个可确认的候选时间。
- 候选时间结合任务预计时长、已确认课程、已有定时任务和当前精力，并为固定安排前后保留 15 分钟缓冲。
- 只有用户点选候选时间后才会写入日程和创建提醒；保持弹性不会改变任务。
- 初步安排会标记来源并保留原有改期能力，不把建议伪装成不可移动的预约。

## 2.4

- 今日页新增本地保存的精力状态：偏低时优先短任务，充足时优先较完整或目标相关的任务；固定日程与逾期提醒不被改写。
- 校园地点改为“应用自动提供优先”，紫金港用户无需制作地点包；JSON 导入收进高级迁移入口。
- 地点帮助中补充后续地图流程：自动搜索校区 POI、按类型推断用途，以及地图点选后的逆地理编码建议。
- V3 路线图加入自动校园 POI 发现和地图点选补充地点；真实地图 SDK 将等待开发者 Key、隐私告知和用户同意后启用。

## 2.3

- “今日”中的收集箱改为摘要加副页面入口；完整收集箱沿用计划副页面的无返回箭头、滑入淡入、系统返回和底栏状态反馈。
- 地点包标题旁新增圆形问号入口，说明 JSON 文件结构、分区代码、UTF-8 保存方式与系统文件选择器导入步骤。
- 路线预览可记录或更新实际总耗时，并可恢复初始估计；校正按出行方式和双向分区组合保存在本机。
- 课程空挡、目标候选时间和路线预览统一使用已确认的路线校正值，不读取后台定位。

## 2.2

- 设置页新增“校园生活”总开关；关闭时收起校内出行、地点包和手动位置工具，但保留课程与本地配置。
- 设置页新增校园地点包导入：接受经过校验的 JSON 文件，限制地点数量、校验分区并拒绝重复名称。
- 导入地点直接进入课程编辑器；地点所属分区继续参与课程空挡和校内路程估算，而非仅用于展示。
- 新增手动当前位置和路线预览；不申请系统定位权限，不后台追踪，也不自动改变日程。
- V3 路线图新增“开始活动建议预计时长／结束时间”，并明确通勤学习只使用用户确认的实际耗时。

## 2.1

- 从“开始活动”中移除不必要的“通勤”类别；通勤信息仍只用于课程空档和校内路程估算。
- “下一件合适的事”改为可解释推荐：依次检查刚错过的任务、90 分钟内的固定安排，以及能在下一项安排前完成并保留缓冲的弹性任务。
- 推荐卡直接显示选择原因；推荐只提供参考，不会自动开始、完成或移动任何任务。
- 下一阶段将加入手动精力状态、地点适配、最低版本与基于反馈的推荐校正。

## 2.0

- 在 1.8 信息架构与交互基础上整合 V2 活动模式，不恢复旧版导航和计划页。
- 开始活动时可选择类别、自定义名称、预计时长或结束时刻，并填写活动结束后的下一步。
- 今日页显示活动倒计时、预计结束时间、延长次数及当日活动记录。
- 到点后提供确认结束、开始下一步、限次延长和重新安排四类转场选择。
- 支持可调提前预告、到点提醒强度、连续延长上限及 Android 精确提醒权限入口。
- 活动与提醒状态写入本地存储，并在应用启动、系统重启或应用更新后恢复。
- 通知支持结束活动、延长十分钟、打开转场；重新安排会进入今日页内的收集箱。
- 设置页新增海盐蓝、薄荷绿、暖杏与暮紫四套主题；页面、导航、卡片、控件和日程色块会整体换色并记住选择。

## 1.8

- 移除日、周时间轴的当前时刻红线。
- 计划主页与副页面加入左右滑动淡入过渡，底栏计划项同步显示当前副页面。
- 计划副页面采用无返回文字和箭头的简洁标题；可通过系统返回键或再次点击底栏“计划”回到目录。

## 1.7

- 将底部导航调整为“今日、日程、计划、设置”，中央加号继续作为独立快速记录动作；收集箱整合进今日概览。
- 今日日程与今日概览分离，日／周时间轴集中到独立“日程”页。
- 计划主页改为无滚动的模块入口；课程、空挡建议、目标与执行、本周回顾、暂停项目进入各自可滚动副页面。
- 教程资料并入“目标与执行”，空挡建议从课程管理中独立出来。
- 周日程维持七天同屏且无横向滑动，同时压缩时间轴与间距、显著加宽色块。
- GitHub Actions 增加拉取请求编译验证，版本号提升到 1.7。

## 1.6

- 紧急修复底部中央加号容器撑满屏幕、导致所有页面内容不可见的问题。
- 保留 1.5 的计划页折叠、周日程适宽、06:00–24:00 时间轴和 V2 活动模式规划。

## 1.5

- 计划页条目移除“展开／收起”文字，只保留方向箭头；每次进入计划页时全部条目默认折叠。
- 周日程改为与日日程相同的屏幕宽度，七天按可用宽度平均分配，不再需要横向滑动。
- 快速记录加号从页面右下角移到底部“收集箱”和“计划”之间，避免遮挡正文和按钮。
- 日、周时间轴范围由 08:00–22:00 扩展为 06:00–24:00。
- 将“活动计时、分级预告与结束转场”加入 V2 路线图，并保存完整实现方案。

## 1.4

- 计划页改为纵向堆叠的可展开条目，首屏先显示课程、教程、目标、回顾和暂停项目的摘要。
- “课程与空档”内部按待确认课程、已确认课程、课程间空档依次堆叠，减少信息混杂并保留确认、忽略和编辑操作。
- 周日程色块移除内部文字并收窄；仍可点击色块查看完整名称、地点与精确时间。
- 周日程新增“显示课程信息”开关，按需在表格下方显示课程名称、地点和起止时间。
- 已完成的定时任务继续保留在日、周日程中并灰化；详情中显示完成状态且不再出现重复完成按钮。

## 1.3

- 今日页新增“日／周”日程切换入口；周视图按周一至周日横向排布。
- 日、周日程统一改为 08:00–22:00 连续时间轴，整点线固定显示，色块上下界对应真实起止时间。
- 课程按实际节次时间绘制；安排任务时可选择预计用时，任务色块高度按分钟计算。
- 点击任意课程或任务色块可查看类型、星期与精确起止时间；任务可从详情直接完成。
- 今日未指定时段的待办与固定日程分开显示，并增加当前时间红线，减少日程误读。
- 每周多次目标显示“已完成、已安排、还需安排”，每一次生成独立任务并可分别完成或改期。
- 增加无课程空档时的备用时段建议，让每周多次目标仍可逐次安排。
- Actions 改用固定的原型签名，并给 APK 与构建产物加入版本号；从 1.3 起后续版本可以直接覆盖安装。

## 1.2

- 弹性安排中的每项任务按实际类型显示对应标签和颜色，避免“弹性”文字与锻炼、休息等颜色不一致。
- 任务卡改为浅色底与窄色条，保留类型辨识度并减少大块灰绿色边框。
- 任务 ID 改为随机唯一值，修复完成一项时多个任务同时消失的问题。
- 自动迁移手机中已有的重复 ID；若多项因该问题被同时完成，仅保留第一项为已完成并恢复其余任务。

## 1.1

- 今日页改为始终可见的按小时日程表；没有事件时仍显示空档，而不再只显示空白表头。
- 重设事件配色与整体背景，课程、学习、锻炼和任务使用清晰但不过度刺眼的颜色。
- 收集箱每个项目提供“安排时间、编辑、删除”三个直接操作；安排后会转为日程任务。
- 新增目标弹窗可滚动，完整显示最低版本建议与创建按钮。
- 计划页顶端增加“从结果开始”入口，明确目标、最低版本和首次安排之间的关系。

## 1.0

- 主页面改为可横向查看的周日程表，课程、学习、任务、运动、娱乐与休息使用不同颜色。
- 所有主要页面支持纵向滚动，避免内容被遮挡。
- 收集箱项目可直接选择时间并加入日程。
- 新增目标先填写预期结果，应用提供可执行的最低版本建议。
- 已完成任务记录完成时间，并在今日页显示今日与本周完成统计。
- 地点功能收回到“设置 → 通勤与地点”，不再作为难以理解的独立页面。
- 修复周日程表的 Kotlin 编译错误。

后续每一次功能更新依次递增 0.1（1.1、1.2……）。
