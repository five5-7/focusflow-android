# FocusFlow 版本记录

## 2.8

- 收集箱“安排时间”升级为推荐空档、大致时间和精确时间三种方式。
- 推荐空档复用现有排程能力，避开已确认课程和未完成的定时任务，并保留 15 分钟缓冲。
- 大致时间支持今天下午、今天晚上、明天上午、明天下午和本周内等范围，不伪装成精确时刻。
- 弹性时间范围作为独立字段保存在本机；后续初步安排只会在该范围内寻找候选。
- 日程页会显示弹性范围，并可重新调整范围或确认具体候选；只有确认具体时间后才创建提醒。
- 开发计划加入 2.9 低打扰状态签到，以及 V3 基于用户确认记录的时段精力与活动学习。

## 2.7

- “下一件合适的事”从只读建议升级为可执行入口，可直接开始、改时间或确认完成。
- 目标任务显示“最低版本”入口，按目标预计时长生成 5–15 分钟的低压力活动预填。
- 当当前精力偏低，或近期反馈集中在“时间不够／精力不足”时，推荐理由会明确提示最低版本。
- 从推荐开始活动会预填名称、类别、预计时长和下一项固定安排；开始前仍可修改。
- 开始活动不会自动把任务标记完成，活动结束后仍由用户明确确认，避免误记完成情况。

## 2.6

- 课程副页面新增课表截图导入，使用设备端中文文字识别，不上传课表图片。
- 识别结果只生成“待确认课程”，不会直接写入周日程或参与空挡计算。
- 结合截图中的星期列、节次行、课程名称与教学楼生成草稿，并过滤已有课程，避免重复导入。
- 待确认课程新增“编辑并确认”，方便修正识别出的名称、节次和教学楼；原有手动新增继续保留。
- 对不含课程文字、星期或节次的截图给出明确提示，不以猜测结果替代确认。

## 2.5

- 日程页的弹性任务新增“初步安排”，在未来七天内生成最多三个可确认的候选时间。
- 候选时间结合任务预计时长、已确认课程、已有定时任务和当前精力，并为固定安排前后保留 15 分钟缓冲。
- 只有用户点选候选时间后才会写入日程和创建提醒；保持弹性不会改变任务。
- 初步安排会标记来源并保留原有改期能力，不把建议伪装成不可移动的预约。

## 2.4

- 今日页新增本地保存的精力状态：偏低时优先短任务，充足时优先较完整或目标相关的任务；固定日程与逾期提醒不被改写。
- 校园地点改为“应用自动提供优先”，紫金港用户无需制作地点包；JSON 导入收进高级迁移入口。
- 地点帮助中补充后续地图流程：自动搜索校区 POI、按类型推断用途，以及地图点选后的逆地理编码建议。
- V3 路线图加入自动校园 POI 发现和地图点选补充地点；真实地图 SDK 将等待开发者 Key、隐私告知和用户同意后启用。

## 2.3

- “今日”中的收集箱改为摘要加副页面入口；完整收集箱沿用计划副页面的无返回箭头、滑入淡入、系统返回和底栏状态反馈。
- 地点包标题旁新增圆形问号入口，说明 JSON 文件结构、分区代码、UTF-8 保存方式与系统文件选择器导入步骤。
- 路线预览可记录或更新实际总耗时，并可恢复初始估计；校正按出行方式和双向分区组合保存在本机。
- 课程空挡、目标候选时间和路线预览统一使用已确认的路线校正值，不读取后台定位。

## 2.2

- 设置页新增“校园生活”总开关；关闭时收起校内出行、地点包和手动位置工具，但保留课程与本地配置。
- 设置页新增校园地点包导入：接受经过校验的 JSON 文件，限制地点数量、校验分区并拒绝重复名称。
- 导入地点直接进入课程编辑器；地点所属分区继续参与课程空挡和校内路程估算，而非仅用于展示。
- 新增手动当前位置和路线预览；不申请系统定位权限，不后台追踪，也不自动改变日程。
- V3 路线图新增“开始活动建议预计时长／结束时间”，并明确通勤学习只使用用户确认的实际耗时。

## 2.1

- 从“开始活动”中移除不必要的“通勤”类别；通勤信息仍只用于课程空档和校内路程估算。
- “下一件合适的事”改为可解释推荐：依次检查刚错过的任务、90 分钟内的固定安排，以及能在下一项安排前完成并保留缓冲的弹性任务。
- 推荐卡直接显示选择原因；推荐只提供参考，不会自动开始、完成或移动任何任务。
- 下一阶段将加入手动精力状态、地点适配、最低版本与基于反馈的推荐校正。

## 2.0

- 在 1.8 信息架构与交互基础上整合 V2 活动模式，不恢复旧版导航和计划页。
- 开始活动时可选择类别、自定义名称、预计时长或结束时刻，并填写活动结束后的下一步。
- 今日页显示活动倒计时、预计结束时间、延长次数及当日活动记录。
- 到点后提供确认结束、开始下一步、限次延长和重新安排四类转场选择。
- 支持可调提前预告、到点提醒强度、连续延长上限及 Android 精确提醒权限入口。
- 活动与提醒状态写入本地存储，并在应用启动、系统重启或应用更新后恢复。
- 通知支持结束活动、延长十分钟、打开转场；重新安排会进入今日页内的收集箱。
- 设置页新增海盐蓝、薄荷绿、暖杏与暮紫四套主题；页面、导航、卡片、控件和日程色块会整体换色并记住选择。

## 1.8

- 移除日、周时间轴的当前时刻红线。
- 计划主页与副页面加入左右滑动淡入过渡，底栏计划项同步显示当前副页面。
- 计划副页面采用无返回文字和箭头的简洁标题；可通过系统返回键或再次点击底栏“计划”回到目录。

## 1.7

- 将底部导航调整为“今日、日程、计划、设置”，中央加号继续作为独立快速记录动作；收集箱整合进今日概览。
- 今日日程与今日概览分离，日／周时间轴集中到独立“日程”页。
- 计划主页改为无滚动的模块入口；课程、空挡建议、目标与执行、本周回顾、暂停项目进入各自可滚动副页面。
- 教程资料并入“目标与执行”，空挡建议从课程管理中独立出来。
- 周日程维持七天同屏且无横向滑动，同时压缩时间轴与间距、显著加宽色块。
- GitHub Actions 增加拉取请求编译验证，版本号提升到 1.7。

## 1.6

- 紧急修复底部中央加号容器撑满屏幕、导致所有页面内容不可见的问题。
- 保留 1.5 的计划页折叠、周日程适宽、06:00–24:00 时间轴和 V2 活动模式规划。

## 1.5

- 计划页条目移除“展开／收起”文字，只保留方向箭头；每次进入计划页时全部条目默认折叠。
- 周日程改为与日日程相同的屏幕宽度，七天按可用宽度平均分配，不再需要横向滑动。
- 快速记录加号从页面右下角移到底部“收集箱”和“计划”之间，避免遮挡正文和按钮。
- 日、周时间轴范围由 08:00–22:00 扩展为 06:00–24:00。
- 将“活动计时、分级预告与结束转场”加入 V2 路线图，并保存完整实现方案。

## 1.4

- 计划页改为纵向堆叠的可展开条目，首屏先显示课程、教程、目标、回顾和暂停项目的摘要。
- “课程与空档”内部按待确认课程、已确认课程、课程间空档依次堆叠，减少信息混杂并保留确认、忽略和编辑操作。
- 周日程色块移除内部文字并收窄；仍可点击色块查看完整名称、地点与精确时间。
- 周日程新增“显示课程信息”开关，按需在表格下方显示课程名称、地点和起止时间。
- 已完成的定时任务继续保留在日、周日程中并灰化；详情中显示完成状态且不再出现重复完成按钮。

## 1.3

- 今日页新增“日／周”日程切换入口；周视图按周一至周日横向排布。
- 日、周日程统一改为 08:00–22:00 连续时间轴，整点线固定显示，色块上下界对应真实起止时间。
- 课程按实际节次时间绘制；安排任务时可选择预计用时，任务色块高度按分钟计算。
- 点击任意课程或任务色块可查看类型、星期与精确起止时间；任务可从详情直接完成。
- 今日未指定时段的待办与固定日程分开显示，并增加当前时间红线，减少日程误读。
- 每周多次目标显示“已完成、已安排、还需安排”，每一次生成独立任务并可分别完成或改期。
- 增加无课程空档时的备用时段建议，让每周多次目标仍可逐次安排。
- Actions 改用固定的原型签名，并给 APK 与构建产物加入版本号；从 1.3 起后续版本可以直接覆盖安装。

## 1.2

- 弹性安排中的每项任务按实际类型显示对应标签和颜色，避免“弹性”文字与锻炼、休息等颜色不一致。
- 任务卡改为浅色底与窄色条，保留类型辨识度并减少大块灰绿色边框。
- 任务 ID 改为随机唯一值，修复完成一项时多个任务同时消失的问题。
- 自动迁移手机中已有的重复 ID；若多项因该问题被同时完成，仅保留第一项为已完成并恢复其余任务。

## 1.1

- 今日页改为始终可见的按小时日程表；没有事件时仍显示空档，而不再只显示空白表头。
- 重设事件配色与整体背景，课程、学习、锻炼和任务使用清晰但不过度刺眼的颜色。
- 收集箱每个项目提供“安排时间、编辑、删除”三个直接操作；安排后会转为日程任务。
- 新增目标弹窗可滚动，完整显示最低版本建议与创建按钮。
- 计划页顶端增加“从结果开始”入口，明确目标、最低版本和首次安排之间的关系。

## 1.0

- 主页面改为可横向查看的周日程表，课程、学习、任务、运动、娱乐与休息使用不同颜色。
- 所有主要页面支持纵向滚动，避免内容被遮挡。
- 收集箱项目可直接选择时间并加入日程。
- 新增目标先填写预期结果，应用提供可执行的最低版本建议。
- 已完成任务记录完成时间，并在今日页显示今日与本周完成统计。
- 地点功能收回到“设置 → 通勤与地点”，不再作为难以理解的独立页面。
- 修复周日程表的 Kotlin 编译错误。

后续每一次功能更新依次递增 0.1（1.1、1.2……）。
