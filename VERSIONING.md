# FocusFlow 版本规则

本文件是 FocusFlow 版本命名与发布状态的唯一规则来源。应用版本值以
`app/build.gradle.kts` 为准；`CHANGELOG.md` 记录实际变更；应用内路线图只做摘要。

## 版本名称

- 正式版使用 `主版本.次版本.修订版本`，例如 `7.2.0`。
- 候选版使用 `主版本.次版本.修订版本-rc.序号`，例如 `7.2.0-rc.1`。
- Actions 的 `run-N` 只标识一次 CI 构建，不是产品版本，也不能表示稳定性。
- 只有已经建立 GitHub Release 的版本才能称为“正式发布”或“Latest”。

## 何时升级

- 主版本：核心数据模型、产品定位或兼容边界发生重大变化。
- 次版本：完成一个经过冻结、能够独立验收的功能主题；不能只因出现新想法而升级。
- 修订版本：某个正式版发布后，仅包含兼容修复、稳定性修复或小范围体验修正。
- `rc.N`：同一冻结范围内的候选迭代。修复候选问题时增加 N，不提前创建新的正式版本号。

## versionCode

- 每个曾分发用于安装的 APK 都必须使用更大的 `versionCode`，不得复用或回退。
- 候选转正式时仍需增加 `versionCode`，确保正式包可以覆盖候选包。
- `versionCode` 与语义版本独立；它只表达 Android 安装升级顺序。

## 发布流程

1. 冻结一个可独立验收的版本范围，并在功能分支实现；同时为该版本写应用内更新说明（`QuickStartDialog.updateHighlightsFor`）的核心变化，禁止落入兜底文案。
2. 将首个可安装候选标为 `x.y.z-rc.1`，运行 debug/release 单测、release 构建、签名和不可调试检查。
3. 修复候选缺陷时依次使用 `rc.2`、`rc.3`，每个分发包增加 `versionCode`。
4. 完成覆盖安装、旧数据、目标设备通知和至少一天连续使用验收。
5. 将 `versionName` 改为 `x.y.z`、再次增加 `versionCode`，重新执行全部构建门禁。
6. 只有在维护者明确批准后才能合并、打 tag 并建立 GitHub Release。

## 命名与状态

- 功能分支建议使用 `agent/focusflow-x.y-主题`。
- 正式 tag 使用 `vx.y.z`；候选包不创建正式 tag。应用内检查更新只接受此标识（tag 形如 `vX.Y.Z` 且非 prerelease 的 GitHub Release）。
- APK 使用 `FocusFlow-<versionName>-run-<Actions编号>.apk`。
- 状态依次为：计划中 → 候选 → 已发布；被新候选替代的包标为“已废弃”，不删除历史记录。
- 路线图可以记录以后要讨论的方向，但不得为尚未冻结的想法预占版本号。

## 当前版本

- 当前开发候选为 `8.2.1-rc.2` / versionCode 527：在 8.2.0 可选外观系统上继续扩展亚克力与毛玻璃，并修正毛玻璃圆角描边、亚克力多余边框及相关说明文字；丰富外观总开关仍默认关闭。需通过 CI 与目标设备视觉／性能验收后，才可转为 8.2.1 正式版。
- 当前正式发布版本为 `8.2.0` / versionCode 525（第四个正式版），GitHub Release tag 为 `v8.2.0`。
- `8.0.0` / versionCode 513 已建立 `v8.0.0` GitHub Release；其 rc.4 基线通过 Run 306 与维护者针对性验收。
- `8.1.0`（versionCode 520，统一动效规范、副页深度缩放转场、底栏形变与选中色块、退出确认与页面历史、检查更新）由 `agent/focusflow-8.0.0` 分支的 rc.6 候选转正：84 个测试类 / 484 条用例通过，稳定签名 release 包已通过真机验收；rc.1~rc.6 已废弃。
- `8.1.1`（versionCode 521）为 8.1.0 的修订版：修「上一步／下一步」把弹窗当独立一步的问题——按维护者口径，「副页 + 弹窗 + 已填数据」是一个整体、打开弹窗本身不计入历史；另含弹窗柔光阴影与页面／卡片分层。属**发布后直补**：维护者指示直接以 8.1.1 发出，未再走 rc 候选流程（与平时流程不同之处记录在此）。
- `8.2.0`（versionCode 525）为可选外观系统：页面背景四档（跟随主题／主题渐变／固定颜色／自选图片）、
  渐变配色与强度可调并支持「跟随内容」、卡片材质四档、课表与日程表底色可单独设置、新增三套内置主题、
  自定义主题工具升级（实时预览／共用外观控件／预设可带整套外观）。维护者已批准本版范围，
  同样直接从 `8.1.1` 转正，未走 rc 候选流程。**发布前置条件已全过**：默认外观与 8.1.1 逐像素一致
  （同机 A/B `meanAbsDiff 0.37`）、95 个测试类 / 575 条单元测试全绿、稳定签名 Release 构建通过。
  见 `docs/8.2.0-release-notes.md` 与 `docs/8.2.0-checklist.md`。状态：**已发布**（第四个正式版，tag `v8.2.0`）。
- rc.1~rc.5 的真机手感数据来自 debug 包，不能作为发布验收证据；按 AGENTS.md 只使用稳定签名的产物做安装验收。
- 8.0.0 / versionCode 511 的首次合并构建已被 rc.4 日期修复替代，不作为发布产物。
- `7.2` 至 `7.12` 的未发布候选均已作为历史输入并入 8.0，不再单独发布。
- 上一个正式 Release 为 `7.1.3`。
