# FocusFlow 8.2.1 · 亚克力与毛玻璃扩展

> 当前候选：`8.2.1-rc.1` / versionCode 526。当前正式版仍为 8.2.0。

## 冻结范围

本版本只扩展 8.2.0 的可选卡片材质，不改变日程、提醒、数据结构或默认外观。
「丰富的动画与外观效果」继续默认关闭；关闭时卡片按原有纯色路径绘制，已经选择的材质值保留。

| 材质 | 背景模糊 | 面层 | 边缘 | 目标观感 |
|---|---:|---|---|---|
| 亚克力 | 18dp | 16% 主题染色，alpha 0.60 | 无独立描边 | 有色塑料板，保留较多背景轮廓 |
| 毛玻璃 | 30dp | 低染色白纱，alpha 0.48 | 1dp 白色内边 + 窄顶部高光 | 中性磨砂玻璃，背景扩散更强 |

普通卡片按页面坐标重画并模糊它覆盖的背景；真正叠在内容之上的悬浮底栏与页内弹窗则共享页面捕获源，
按屏幕坐标截取它们实际挡住的日程文字、色块与背景，再做模糊和半透明染色。正文、按钮和交互区域
始终在效果层之后绘制，不参与模糊。捕获源只在丰富外观开启且选择亚克力／毛玻璃时建立。

## 数据兼容

- 继续使用 SharedPreferences 键 `card_material`。
- 既有 `acrylic` 原样读取；新增 `frosted`。
- 已删除的 `paper` 与未知值继续降级为 `tonal`，不删除、不重置其它用户数据。
- 主题预设继续通过既有 `appearance.cardMaterial` 字段保存，无需迁移 JSON 结构。

## 候选验收

- [x] 丰富外观默认关闭；旧安装缺少该键时也读取为关闭。
- [x] 亚克力与毛玻璃均有独立标签、存储值和非空绘制层。
- [x] 两档的模糊半径、面层透明度与染色有自动化断言；亚克力无描边，毛玻璃描边沿真实圆角闭合。
- [x] 悬浮底栏与页内弹窗使用真实页面捕获源，能透出并模糊被它们遮住的内容；无捕获源时安全回退。
- [x] 应用内更新说明、帮助、说明书、路线图和 CHANGELOG 已同步。
- [ ] Debug/Release 单元测试全绿。
- [ ] Debug/Release APK 构建通过，稳定签名与版本元数据通过 CI 校验。
- [ ] OPPO / ColorOS 16 / Android 15 真机对照：图片背景与主题渐变各检查一次浅色／深色。
- [ ] 真机确认文字清晰、圆角无溢出，滚动与页面切换无明显掉帧。

## 转正条件

真机视觉与性能通过后，将版本提升为 8.2.1、递增 versionCode，再重新执行全部门禁。
只有维护者明确批准后才合并、打 `v8.2.1` tag 和创建正式 Release。
