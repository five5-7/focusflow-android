# FocusFlow 8.1.0 审计报告

审计对象：`agent/focusflow-8.0.0` 分支，HEAD `20a3b40`（本会话 14 个提交，`git diff 7e33be8..HEAD -- app/src` = 12 文件 / +659 −130）。
方法：三路只读审查（代码级、导航规则与测试、发布流程）+ 真机测量（release 包，1440×3168 / density 2.75：截图差分、像素定位、gfxinfo 帧统计、simpleperf 采样）。
状态：**报告与修复清单**。尚未按本报告改动任何代码，等维护者确认后再改。

---

## 一、结论摘要

- **数据契约零风险**：14 个提交未触碰 `PrototypeStore` 与任何 `*Data`/`*Store`；SharedPreferences 键、JSON 字段无新增/删除/语义变化。
- **临时实验已清干净**：方案切换开关（`ExitScheme`）已彻底删除，`MotionScale` 回到只有 `durationScale`。
- **动画速度覆盖**：main 源码中已无裸 `tween(`，全部经 `MotionSpec` → `motionMillis`；0× 时全部退化为瞬时（单测覆盖）。
- **但存在 1 条会静默吞掉用户点击的严重回归**（本报告第二节，已真机复现），另有 5 条中等问题、16 条低优先问题。

---

## 二、严重（必修，用户可感）

### S1. 离开带子页的页签后，页面点击被静默吞掉约 400ms

- 位置：`MainActivity.kt:987`（`shrinkingOnTop`）、`990-996`（zIndex 2f）、`1008-1015`（消费型 pointerInput）、`420-425`（复位窗口）。
- 机理：收起中的隐藏页签被抬到 `zIndex(2f)`（可见页签只有 `1f`），同时它仍挂着 `pointerInput` 在 `Initial` 阶段消费所有指针事件；Compose 同级重叠命中只走最上层 → 收起期间页面上的点击先到它并被吞掉。`graphicsLayer` 只让命中区随缩放变小，**alpha=0 不关闭命中**。
- 复位窗口 `delay(MotionSpec.SUBPAGE_CROSS_MS + 80L)` 是**真实毫秒**，不随「外观 → 动画速度」缩放：0.5× 时动画 160ms 结束却仍挡 400ms；速度=关闭时动画瞬时，照样挡 400ms。
- **真机复现（release 包）**：
  ```
  计划·课程 → 点今日 → 立刻点「开始活动」   → 弹窗未打开（点击被吞）
  计划·课程 → 点今日 → 等 1.8s 再点同一位置 → 弹窗正常打开
  ```
- 修法：收起页签只负责绘制、不参与输入（`if (isVisibleTab || collapseLeaving) Modifier else Modifier.pointerInput…`）；复位窗口改用 `delay(motionMillis(SUBPAGE_CROSS_MS + 80))`；若要保留"缩小副页压在目标页之上"，改为 可见=2f / 收起=1f，靠副页层内部 `zIndex(1f)` 维持层级。

---

## 三、中等

| # | 位置 | 问题 | 影响 | 修法 |
|---|---|---|---|---|
| M1 | `MainActivity.kt:2160-2169` | 历史列表跳转（长按回退键弹列表）只调 `navHistory.jumpTo` + `applySnapshot`，**漏调 `prepareNavigation`** | 离开页不播收起；该抑制的"补播缩小"反而播出；残留 `leavingSubpageTab` 可能误伤其他页签 | 补 `prepareNavigation(navHistory.current)`，或把规则收进 `applySnapshot` 唯一入口 |
| M2 | `FloatingNavigationBar.kt:175-198` | `indicatorSlot.value` 在**组合阶段**读取 | 每次切页签的 260ms 内整条底栏逐帧重组（与本会话"切换不掉帧"目标冲突） | 读值下沉到 `Modifier.offset{}` / `layout{}` / `drawBehind` |
| M3 | `FloatingNavigationBar.kt:142-149` | 底色回弹用硬编码 `spring(...)`，绕过 `MotionSpec`；且触发条件从"选中项目的地变化"变成"`selectedPageDescription` 变化" | 「动画速度」不覆盖它；每次切页签都会弹一下（比基线更频繁） | 新增 `MotionSpec.pulse()`；恢复"仅子页名变化时"触发 |
| M4 | `MainActivity.kt:412-417`、`420-425` | `pageSnapToken` / `leavingSubpageTab` 用 360ms / 400ms **真实毫秒窗口**复位 | 窗口内该页签的子页切换被误抑制（直达回主页后 360ms 内再开子页 → 无动画）；倍率 0.5× 时窗口相对更长 | 窗口随 `motionMillis` 缩放，或把"不补播"绑到一次导航的身份而非计时器 |
| M5 | `MotionSpec.kt:16` vs `SubpageMotion.kt:47-60`、`85-97` | 文档声称"副页进出严格互逆"，实现有三处不对称：240ms vs 260ms；进入无淡入而返回有 `fadeOut`；主页退让原点（Center）与副页收放原点（底栏图标）不同源。另 `enterSubpage` 里作用于 `null` 分支的 `scaleOut(0.96)` 是空转 | 规范误导后续维护；"同一坐标点"的深度观感不成立（4% 缩放，视觉影响小） | 统一时长与原点，或把文档改成如实描述；删掉空转的半条转场 |
| M6 | `NavigationHistory.kt:26` | `isTabRoot()` 是**全局字段**判定，与 `onCurrentSubpage`（`MainActivity.kt:785-787`）、`PageSnapshot.label` 的按页签口径不一致 | 后台残留子页会让"主页↔主页不记录历史"失效，并让 `markWorkedHere` 失效 | 改为按当前页签判定 |

---

## 四、低优先（16 条，摘要）

- `MainActivity.kt:409/419-421` 用字面量 `-1` 代替 `TabMotionRules.NO_TAB`。
- `TabMotionRules.kt:44` 的 `0.85f` 是规则文件里的魔法数 → 提到 `MotionSpec.JUMP_REST_SCALE`。
- `MainActivity.kt:952` 的 `80.dp/96.dp` 平动幅度硬编码 → 收进 MotionSpec/Layout。
- `MainActivity.kt:923` 注释"四个页签常驻组合（零重组）"已过期。
- `MainActivity.kt:933-944` 新页签在 `LaunchedEffect(tab)` 的**下一帧**才入 `visitedTabs` → 首次进入空白一帧；建议组合期 add。
- `MainActivity.kt:420-425` 快速连点两个页签时 `leavingSubpageTab` 被复位 → 前一个页签的收起中途失去 `collapseLeaving`，`tabX` 目标从 0 变 ±80dp，半透明缩小时横向抖动。
- `MotionSpec.kt:25/31` 注释提到已不存在的"子页推入"；"选中态"实际用 `move()`。
- `MotionSpec.kt:43-47` `COLLAPSE_SCALE=0.12` 注释称"约等于底栏图标底色大小"，实际是 0.12×宽 × 0.12×高的竖条。
- `SubpageMotion.kt:91-96` 子页之间父子上退时，被收起的子页仍收向**底栏图标**（应为上一层父页中心）；设置 `CAMPUS_PLACES(3)→APPEARANCE(1)` 会走到。
- `SubpageMotion.kt:116` 组合期读 `transition.isRunning/currentState` 挂拦截层（基线已有）。
- `MainActivity.kt:1543-1548` 的 `hasSubpage` 与 `TabMotionRules.subpageOpenOn` 是同一规则两份实现 → 改调后者。
- 未使用 import：`MainActivity.kt` 约 35 个、`PlansScreen.kt` 约 50 个、`TodayScreen.kt`/`SettingsScreen.kt` 若干、`ScrollableContainers.kt:23`（均为基线遗留）。
- `NavKind` 的 `TAB/BACK/FORWARD` 只写不读（全仓只有 `== JUMP` 一处读）。
- `MotionScale.kt:22` `motionMillis` 唯一调用点是 `MotionSpec`。
- `FloatingNavigationBar.kt:351-354` `fill` 由动画中的 progress 算、`foreground` 再对它做 `animateColorAsState`，形成双层延迟，切换瞬间图标可能低对比。
- `MainActivity.kt:139-142` 冷启动返回兜底 `addCallback` 永不注销 → 若将来某状态无 Compose BackHandler，返回会被永久吞掉。

---

## 五、发布缺口（按项目自身规则）

| # | 缺口 | 后果 |
|---|---|---|
| G1 | `app/build.gradle.kts` 的版本串未出现在 `README.md:3` 与 `AGENTS.md:35`（CI 第一步就校验这个） | **任何 PR 到 main 都会在 CI 首步失败**（功能分支不触发 CI 所以尚未暴露） |
| G2 | `CHANGELOG.md:3-8` 未覆盖 rc.5 之后的 12 个提交（动效规范、深度缩放、底栏色块、TabMotionRules、退出提示时长、冷启动兜底、按需组合、memo 化） | 版本记录与实际不符 |
| G3 | `README.md:39/40/41/43/17/46` 与实现不符（平移/深度缩放/圆环/色块平移/动画速度/接受候选版/测试规模） | 文档误导 |
| G4 | `RoadmapData.kt:23-25` 只有 rc.5 条目；`:421` 标题硬编码"1.0 → 8.0" | 应用内路线图过期 |
| G5 | `QuickStartDialog.kt:105-110` 未体现 8.1 动效核心变化 | 违反 AGENTS「更新说明必须展示该版本核心变化」 |
| G6 | `VERSIONING.md:45-46` 与已存在的 tag `v8.0.0` 冲突，且未反映 rc.5 之后的改动 | 版本规则自相矛盾 |
| G7 | 真机验收证据来自 **debug 包**（AGENTS 要求只用稳定签名产物验收） | rc.1–rc.5 的验收不能作为发布证据 |
| G8 | `docs/` 无任何 8.1 文档（无验收清单、无 release notes） | 发布流程缺件 |
| G9 | `docs/unified-design-language-plan.md:35` 规定"150–250ms"，与 `MOVE_MS/MORPH_MS=260`、`SUBPAGE_CROSS_MS=320` 冲突 | 设计规范与实现冲突 |

版本号建议：`8.1.0-rc.6` / versionCode `519`（依据 VERSIONING「rc.N 迭代」+「每个分发 APK 递增 versionCode」）；验收通过后转 `8.1.0` / `520`。

---

## 六、真机回归矩阵（release 包）

已完成：

| 项 | 结果 |
|---|---|
| 底栏选中色块对齐（开启动画） | 中心 246 / 483 / 957 / 1193，页签中心 246 / 482 / 956 / 1194 → 误差 ≤1px |
| 底栏选中色块对齐（动画=关闭） | 落点与开启动画一致（246 / 1193 / 957），无卡死、无崩溃 |
| 子页圆环态 | 实心 27,224 px ↔ 圆环 6,618 px，包围盒一致（约 191×191） |
| 弹窗期间底栏 | 底栏节点**不在无障碍树**、点击被吞（现状确认） |
| 跨页签收起后点击 | **被吞约 400ms**（S1，已复现） |
| 冷启动 / 切换帧 | release：188ms / 90 分位 11ms / 最重帧 34–65ms（debug 分别 517ms / 16ms / 60–150ms） |

待跑（需维护者授权改系统设置或真机手感）：深色模式与 4 套主题下的色块对比度、大字体、窄屏/分屏、横屏、键盘避让、手势/三键导航、通知与后台、重启恢复。

---

## 七、建议的修复批次

- **P0（立即，用户可感）**：S1（吞点击 + 窗口缩放）、M1（历史列表漏 `prepareNavigation`）、M2（底栏逐帧重组）。
- **P1（正确性）**：M4（计时器窗口）、M3（弹簧入 MotionSpec + 触发条件）、`visitedTabs` 空白帧、M6（`isTabRoot` 口径）、`SubpageMotion` 父子上退原点、M5（文档与实现对齐）。
- **P2（发布收尾）**：G1 → G2 → G4 → G5 → G6 → G8，版本号 rc.6 / 519；G7 用 release 包重做验收。
- **P3（清理）**：未使用 import、魔法数、过期注释、`NavKind` 死枚举、`addCallback` 注销。

---

## 八、待维护者决策

1. P0 + P1 是否全部修（建议：P0 立即，P1 同批）。
2. 深链打开设置子页时，子页层是否应该播动画（当前被误判为"顺带重置"而抑制）。
3. 版本号 `8.1.0-rc.6` / `519` 与六处文档同步是否执行（CI 门禁前提）。
4. 弹窗期间历史键不可用：是否按"课程编辑器改为页内浮层"方案改造（其余弹窗保持现状）。
5. 子页之间（如设置多级）是否改为纯平移、不缩放。
