# 7.1.3 候选验收

## 自动检查

- Debug / Release 单元测试、两种构建、稳定签名和非 debuggable 校验。
- 图标资源：v26 普通自适应，v33 增加单色；前景与单色轮廓一致、透明底、所有控制点及描边在安全圆内。
- 静态检查：顶部安全区仅作正文初始 padding，普通状态下不占 Scaffold topBar；弹窗不继承正文留白。

## 必须真机确认（自动构建不能替代）

- 覆盖 7.1.2：版本 7.1.3 / 478，任务、历史、主题、权限和提醒不丢失。
- 今日、日程、计划、设置和各子页面：回到顶部时标题完整、位于状态栏下；慢慢上滑，文字能进入原截断线上方，无固定空白条。
- 渐隐只在系统栏附近，不形成明显色带；深浅主题下时钟、电量等仍清楚。如果系统隐藏状态栏，不应保留该渐隐层。
- 竖屏挖孔、横屏侧挖孔、普通屏、三键/手势导航、分屏及大字体：初始内容不被遮挡；横屏切口侧仍有安全距离。
- AI 加载条 / 数据保护提示出现和消失后布局正确，提示与重试按钮不进入系统栏；不要为测试主动破坏正式数据。
- 输入法、快速入门、连续进入返回、底栏悬浮和内容末尾避让仍正常。
- 四个主入口都有淡入淡出，底栏不跟随页面移动；快速连续点击最终停在最后一次选择的页面，旧页面按钮不被误触。系统关闭动画时立即切换。
- 进入收集箱/计划子页/设置子页：所属入口保持选中、显示小圆点并轻微回弹；深入另一子页继续反馈，返回主页圆点淡出；点击当前底栏入口直接回该主页。
- 从子页切换另一主入口时，原子页淡出前不要闪回旧主页；再次访问主入口时显示其主页，滚动位置合理保留。
- 桌面、最近任务、启动画面中的新图标显示正确；圆形/圆角方形裁切不丢聚焦框。Android 13+ 桌面支持主题图标时切换检查单色轮廓。
- 部分桌面会缓存旧图标：先退出桌面再进入或重启手机；不要卸载应用或清空应用/桌面数据。

不自动合并或正式发布；按用户真机反馈继续调整。
