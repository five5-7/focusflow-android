# 8.2.0 验收清单（可选外观系统）

> 用途：把"每阶段跑单测 + 出真机证据"落成可逐条勾选的清单，避免像 8.2.0 开发过程中那样
> 反复用截图像素推断"到底生效没有"。真机测试时段：每天 17:00 前（维护者约定）。
> 设备：OPPO ColorOS 16 / Android 15，1440×3168。构建：`D:\focusflow\.tmp\build.ps1`。
> 新会话请先读 `docs/8.2.0-handover.md`（交接材料：现状、缺口、命令、踩过的坑）。

## 零、通用前置

- [x] `:app:testDebugUnitTest` 全绿（2026-09-10 复核：94 类 / 571 条 / 0 失败 / 0 跳过）。
- [ ] 安装的是**稳定签名 release 包**，且与 `app/build.gradle.kts` 版本一致。
- [ ] 每条结论附证据：截图文件名 / 像素采样值 / `gfxinfo` 数字，写回本文件对应行。

## 一、不变量（最优先，任何一条不过都不发）

| 检查 | 方法 | 期望 |
|---|---|---|
| 默认外观与 8.1.1 逐像素一致 | **一条命令**：先把外观三项复位，再跑 `powershell -File D:\focusflow\.tmp\invariant_check.ps1`（自动装 8.1.1 截计划页 → 装回 dev 包 → 同页截图 → `pixdiff`）；`-DryRun` 只打印步骤 | 内容/主题/数据完全相同，差异只可能来自外观代码；**别拿几天前的旧图比**（内容不一致会得到 20+ 的假差异，2026-09-10 踩过）。差异偏大时先怀疑"外观没复位" |
| 老装机升级后外观不变 | 覆盖安装（保留数据）后不做任何设置，截图对比 | 与升级前一致 |
| 课程块 / 日程块颜色不受外观影响 | 建一门课→切课表底色→同一课程块取色 | 换底色前后块体取色**完全相同** |
| 原有四套主题取值冻结 | 单测 `ThemeGalleryTest.originalFourThemesArePixelFrozen` | 全绿（已有） |

**✅ 第一行已实测通过（2026-09-10，`invariant_check.ps1` 首次真正跑通）**

- 前置：`reset_appearance.ps1` 先把设备复位，自述行读到
  `theme=海盐蓝 | pageBackdrop=跟随主题`（复位前后都会打印，可核对）。
- 结果：`meanAbsDiff=0.37`、`changed(>12)=0.56%` → **VERDICT PASS**（阈值 ≤1.0）；
  证据图 `D:\focusflow\.tmp\inv_811.png` 与 `inv_dev.png`。
  （首次跑得 0.58，清掉滚动相位死代码后重跑得 0.37。）

**必须把话说准：这 0.37 全部来自底栏描边，不是"外观"差异。** 逐区域实测：

| 区域 | meanAbsDiff |
|---|---|
| **整页上半部分（y<2600）** | **0.00**（逐像素完全相同） |
| 底栏区域（y≥2600） | 2.04 |

差异的成因已经定位到具体像素：8.1.1 的底栏有 `BorderStroke(2.dp, alpha 0.40)`，
边缘是 `(189,197,200)` 的硬线；当前包是 `BorderStroke(0.6.dp, alpha 0.12)` + `shadowElevation 7dp`，
边缘变成 `(221,231,234)` 的柔和过渡。这正是提交 `cc394cd` 按维护者口径做的改动
（**「不要那条 2dp 的硬灰线，改成靠里浅、靠边深的过渡」**），**发生在 8.1.1 发版之后、属于 8.2.0 的既定范围**。

所以这句话的准确含义是：**「外观系统在默认档位下不改变任何渲染」**——
页面上半部分逐像素为 0，底栏的差异是专门要求的描边改款。底栏几何本身也没变
（仍是 x=64..1376、y≈2672..3071，与 8.1.1 一致）。

- **坑**：`invariant_check.ps1` 要用**文件重定向**调用（见 §六 的说明），
  直接管道接输出会挂住不返回；脚本现在会解析 `meanAbsDiff` 并自己给 PASS/FAIL 退出码。


## 二、逐档验证（每档一张截图 + 采色）

- [ ] 页面背景 · 跟随主题：`rgb(242,236,232)`（暖杏浅色）
- [ ] 页面背景 · 主题渐变：顶端→底端级差 ≈22（强度 100%）、≈34（196%）
- [ ] 页面背景 · 固定颜色：选中色板即页面主色（已验 `#F6F1EC`）
- [ ] 页面背景 · 图片：只存本机；不透明度 12% / 97% 两张（已验 97% 偏冷蓝）
- [ ] 从图片抽取主题色：强调色随图片主色变化（已验变蓝）；画面太素时提示且**不改主题**
- [ ] 渐变配色：八组各一张，端色 = 所选「顶色→底色」
- [x] **渐变跟随内容**：已验（2026-09-10 晚，薄荷绿 + 渐变 200%）：关＝滚动前后同一屏幕位置**逐像素完全相同**、
      一屏内级差 13.2；开＝同一位置随滚动变化（每次滑动约 3 级）、一屏内级差只有 **4.8**（1/2.75）。
      与设计跨度 3.2 屏吻合（13.2 / 3.2 ≈ 4.1）。明细见 plan §六「阶段 E」。
- [ ] 卡片材质：默认（逐像素）/ 渐变 / 柔光 各一张（**纸感已于 2026-09-10 删除，本行原写的"纸感"作废**）
- [ ] 课表与日程表底色：跟随主题 / 选颜色 / 图片 / **透明** 四档各一张
      （「透明」档 2026-09-10 新增，浅色像素证据已出，见交接材料第十一节；**深色档还没验**）
- [ ] 七套主题：各一张 + `measure_theme3.ps1` 复测四套主题指示色对比度
- [ ] **自定义主题工具**（第 7 项）：预览块排版正常；改渐变/材质时预览当场变化；
      「从图片抽取主题色」只改工作副本（点「应用此配色」后才换全局）；
      保存预设勾选/不勾选外观两种各存一套，点「应用」的表现符合预期；
      **老预设（升级前存的）应用后背景与卡片不被改动**；删除被预设引用的图片后再应用该预设 → 降级为跟随主题不崩；
      「恢复默认主题配色与外观」把背景与材质一并复位

## 三、深色模式（维护者明确要求）

> **对比度这一半已经离线算全了**：`AppearanceContrastMatrixTest` 覆盖 7 主题 × 浅/深 × 4 档背景
> （图片再分 1% / 50% / 100%），每格取最不利底色，结果表在 `docs/8.2.0-appearance-plan.md` §六「阶段 C/D」。
> 全表最低 6.9:1、不含图片最低 8.1:1。真机只需抽查下面几条与表对照，不必逐格截图。

- [ ] 四档背景在深色下各一张：渐变各点亮度 < 0.35、正文对比度 ≥ 7:1（单测已锁，真机复核）
- [ ] 自选浅色配色 / 固定浅色在深色下被压暗（保留色相、整页仍是深色）
- [ ] 深色下切主题、切背景、开合弹窗均无闪白

## 四、掉帧（`dumpsys gfxinfo framestats`）

> 现成工具：`powershell -File D:\focusflow\.tmp\framestats.ps1 -Label tabs`
> —— 自动 `reset` → 提示你完成手势 → 等待 → 取数 → 打印
> `frames=… janky=…% (budget 5%) p50/p90/p99=…ms verdict=PASS|FAIL`，并按预算返回退出码。
> 解析器已用 6 份历史 dump **离线校验**（数值与原文逐项一致），现场不用再手工数。
> 想复核已有文件：`-FromFile D:\focusflow\.tmp\gfx_fs4.txt`（不需要设备）。

- [x] 页签切换：**2.23%**（493 帧，p90 14ms / p99 40ms）≤ 5%（8.1.1 基线 1.7%）
- [x] 副页放大/缩小：**2.47%**（283 帧，p90 14ms / p99 61ms）≤ 5%（8.1.1 基线 4.2%，比基线更好）
- [x] 长列表滚动：**0.44%**（685 帧，p90 10ms / p99 12ms）≤ 5%
- [x] 弹窗开合：**3.72%**（188 帧，p99 24ms，含一两帧 >20ms，非连续）
- [x] 卡片材质 = 纸感下滚动：**0.98%**（612 帧，p90 12ms / p99 15ms）——比默认材质多约 0.5pp
      ~~本行已作废：纸感材质 2026-09-10 删除~~；现有三档（默认/渐变/柔光）的滚动数据待重取
- [ ] 图片背景下的滚动与页签切换：与纯色对比，差异不超过 1 个百分点
- [ ] **毛玻璃若要做**：必须先出 blur 版本与本表的对比数字，低端机不稳则不做（当前未实现）
      说明：以上是**渐变 200%（最猛档）**下的成绩，重帧 0.44–3.72%、预算 5% 尚有余量；
      但 blur 是每帧抓背景层，不能拿这组数字外推。

## 五、文案一致性

- [ ] 说明书第八章「外观与主题」与实际能力逐条对得上（单测已守 15 个关键词，含第 7 项的实时预览/预设/恢复默认）
- [ ] 帮助（外观区块）、快速上手、路线图 8.2.0 条目、CHANGELOG 8.2.0 小节一致
- [ ] 界面里**不存在**"承诺了却不生效"的开关
- [ ] 更新说明不落兜底：`updateHighlightsFor` 单测已守"已知版本必须 2–3 条、且不等于兜底文案"

## 六、已知未完成（发版前需明确取舍）

1. **渐变跟随内容**：已换实现——渐变改由**滚动内容自己按内容高度**铺（`ScrollableWithContainers` 的
   `ScrollableWithBar`），不再依赖任何滚动事件；视口那层在开启时不再画渐变。开关已回到界面。
   **待真机确认**：滚动时同一屏幕位置的底色应当变化，且整段内容内的颜色变化远小于"固定一屏"。
2. **卡片材质收编**：51 处卡片容器已收编 **32 处**；剩余 19 处**有意留着**（`ElevatedCard` 13、`OutlinedCard` 1、
   `Card(border=…)` 1、`Card(onClick=…)` 1、无显式底色的 `Card` 1、课表/日程表底板 2）。
   前五类改了就不是逐像素一致（会丢阴影/描边/点击重载），**必须先做真机 A/B**；课表底板是有意不收编
   （底色归「课表底色」设置管）。真机 A/B 做法：同一页面同一路径，8.1.1 包与当前包各截一张，`pixdiff.ps1` 比。
3. **毛玻璃**：未实现，等第四节的帧时间实测结论。
4. 底栏两侧箭头位置（当前 TopStart/TopEnd 偏移 −2dp / +2dp、纵向 0dp）待维护者确认是否还要更靠角落。
   **上一步／下一步出现时的顶角圆角已按维护者口径从 10dp 调到 18dp**（`FloatingNavigationBar.kt`
   的 `AsymmetricCapsuleShape(..., smallRadiusDp = 18f)`）：底栏高 92dp、完整胶囊半径 46dp，
   18dp 仍与平时状态有明显形变对比，只是上角不再那么方。
   量法：真机上 10dp = 40px、18dp = 72px（density 4），像素级可比对。
5. ~~**滚动相位那套代码现在成了死代码**~~ → **已清理完毕（2026-09-10 晚，提交 `6f33d0e`）**。
   `SurfaceMaterial.appearanceBackdrop` 在 `Page + GRADIENT + 跟随内容` 时提前 `return this`
   （**这个 return 是必需的**，它把渐变交给 `ScrollableWithBar` 按内容高度铺），所以后面的
   `window`/`phase` 与 `ThemeGradient.page` 的 `windowStops` 分支在生产路径上**永远不可达**，
   `MainActivity` 的 `scrolledScreens` / `scrollTracker` 同样不影响任何像素。
   已删除这套相位代码、其 5 条单测与 `nestedScroll` 挂载（净删 174 行）。
   **验证**：单测全绿；不变量比对 `meanAbsDiff 0.37`（清理前 0.58）→ 渲染等价。
   回退历史在 `cfbfaab` 之前。
   附带修掉一个新发现的缺陷：开关打开时自定义主题的**预览退化成纯色**（提交 `852dfe1`）——
   预览块不是滚动容器，现在按与 `ScrollableWithBar` 相同口径自己补一张渐变。
   **不要**靠删掉那个提前 return 来"修"它。
6. **自定义主题工具（第 7 项）已实现**（提交见 git log `feat(appearance)`）：预览 + 共用外观控件 +
   预设可带整套外观 + 恢复默认连外观复位。剩下的是**真机走查**（见第二节最后一条），
   以及"预览块 176dp 高、预设行多一行说明"这类排版确认。
7. **深色模式下渐变幅度偏小 → 维护者已拍板：保持现状**（2026-09-10 真机发现 + 当场决策）。
   现象：深色 + 主题渐变 200% 时整屏只从 `rgb(68,69,73)` 变到 `rgb(65,66,71)`（**2–3 级**，
   基本看不出是渐变），同一设置浅色下有 13 级；页面本身是深的、正文对比度 15.0:1。
   维护者口径：**「保持现状（深色下克制到接近纯色）」**——这是设计选择，不是待修缺陷；
   不要再为"深色下渐变不明显"改幅度，也不要在清单里当问题追。
8. ~~**柔光待返工（T-1）**~~ → ✅ **已修**：幅度改成按 sRGB 灰阶级数算、逐通道镜像，
   真机实测卡面 `201 → 211 → 220`（底色 `211`）严格 ±10 对称、落差 20 级，亮度中性达标。
9. **底栏跟随左右渐变（T-4）已被回退**：`navBarBrushOverBackdrop` 现在硬编码 `return null`
   （自绘图层与 Surface 裁剪对不齐，实测渲染成三层）。底栏目前走**纯色**路径，
   跟随渐变这件事**没做成**；维护者若问起，要如实回答"试过、有回归、退回来了"，
   不要说成"已实现"。
10. ~~**弹窗材质（「弹窗还是没有渲染」）**~~ → ✅ **已修**：根因是 `drawBehind` 排在 `Surface`
   内部 `.background(color)` 之前、材质被底色整块盖住。`AppDialog` 改成把材质画进 Surface 内容最底层，
   真机实测弹窗卡面 **+18 级**竖向渐变。
11. **底栏材质（尚未做）**：同一个根因，但**不能用同一个修法**——把底栏 Surface 底色让成透明后，
   Surface 自己的 `shadowElevation` 阴影会画到材质层上面，维护者当场复现了"胶囊要么没渲染、
   要么就有问题"的老毛病，**已撤回**。正解是"把材质画进 Surface 内容最底层"，
   但底栏内容是 `BoxWithConstraints` + 横向滚动 + 形变胶囊，插一层 Box 会影响最小约束传播，
   必须单独验证。**当前底栏不显示卡片材质**，这是已知取舍。
12. **p99 偶发长帧（未定位）**：四个场景重帧率都 ≤3.35%（预算 5%），但 p99 达 73–117ms；
   **默认材质下同样是 117ms**，与卡片材质无关。候选是"设置 → 外观"这类重页面的首次组合成本。
13. **弹窗遮罩（维护者新提）**：「弹窗出现时半透明的变化过于简化了，如果底下有字怎么办」。
   **尚未动**：现状是一层固定 alpha 的纯黑遮罩，底下的正文照样看得清。
   方向是给遮罩加**背景模糊**（`Modifier.blur`，minSdk 26、target 35，Android 15 上任）；
   这是逐帧 RenderEffect，**必须先出 `framestats.ps1` 数字**再定去留。
