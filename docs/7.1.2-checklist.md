# 7.1.2 修复候选验收

包名不变，477 / 7.1.2；仅分发同一 CI 验证的稳定签名 release APK，不自动合并/发布。

## 自动验证

- debug/release 单元测试与 APK 构建全部成功；签名验证成功、不可调试。
- 六色往返、旧五色与两种颜色数值格式兼容、预设、缺字段默认、主题派生与文字对比度测试。
- 检查所有 classes*.dex，而非只看 classes.dex 或 APK 大小；确认包含 FloatingNavigationBar 与 ThemeColorsCodec。

## 设备视觉与交互（必须真机确认）

1. 从 7.1.1 覆盖安装，原任务、提醒、主题与预设保留。旧五色自动出现第六色；重启应用后配色不变。
2. 今日/日程/计划/设置及所有子页滚动：内容从悬浮栏后经过，底栏上下/左右没有额外应用底座；最后一项可完整滚到栏上方并点击。
3. 今日和设置选中块均只在图标后，四角完整，点击波纹不碰外框。连续快速切换/系统返回不残影、不触发下面的任务；中央加号独立工作。
4. 点底栏左右空白及下方留白可操作下面的页面；点击栏内非按钮处不应误点下方任务。
5. 子页动画不带动底栏；长列表返回保留滚动位置。弹窗没有重复底栏留白。
6. 四套主题分别检查：暖杏卡片不再偏紫，底栏能改变颜色；深色模式保留主题色调，图标/文字可读。
7. 自定义导航栏色选纯白、纯黑、与主色相同的颜色；选中态、加号、文字均可辨认。保存预设、切换到别的主题、重启、再应用预设，六色一致。
8. 大字体/窄分屏/横屏/挖孔屏/三键导航/手势导航下不遮挡，窄分屏底栏允许横向滚动。大字体能增加高度，列表末尾同步增加避让。
9. 页面输入框聚焦和键盘收起、弹窗输入、Snackbar 均不被底栏遮挡；系统关闭动画后操作立即生效。
10. 复测旧任务完成/改期/统计与后台提前/到点提醒。本次不改变这些逻辑，但不能用单测代替后台验收。

未做模拟器或真机截图验证时，不将布局/触摸通过写入结果。
