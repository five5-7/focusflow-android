# FocusFlow 8.1.0 候选验收

版本：`8.1.0-rc.6` / versionCode `519`。`8.0.0`（513，已发布 `v8.0.0`）仍是当前正式版；本候选未通过验收不得替换正式 Release。

## 自动门槛

- [ ] CI「Verify version metadata」通过：README 含 `**8.1.0-rc.6**` 与 `versionCode 519`、AGENTS 含 `` `8.1.0-rc.6` / versionCode 519 ``，与 `app/build.gradle.kts` 一致。
- [ ] `:app:testDebugUnitTest` 与 `:app:testReleaseUnitTest` 全绿（84 个测试类 / 484 条用例，0 失败 0 跳过）。
- [ ] Debug 与稳定签名 Release 构建通过；release 不可调试；签名与 8.0.0 一致（证书 SHA-256 `650a17f2…`）。
- [ ] APK 元数据为 8.1.0-rc.6 / 519。

## 安装与验收包

- [ ] 只使用稳定签名的产物安装（不再使用 debug 包——此前手感数据来自 debug 包）。
- [ ] 从 8.0.0（513）覆盖安装，以及从 rc.5（518）覆盖安装，均不清除数据。
- [ ] release 包上复测冷启动与切换手感（参考基线：冷启动 188ms、切换 90 分位 11ms、最重帧 34–65ms）。

## 转场与动效

- [ ] 主页↔主页：左右平移、无缩放；来回切换不重播收起动画。
- [ ] 主页→副页：从底栏对应图标**实心**放大展开（无"淡影"）；返回时缩回同一图标并同步淡出。
- [ ] 跨页签（A 副页 → B 主页）：收起吸向目标页签槽位（320ms）；结束后无残留图层。
- [ ] **离开带子页的页签后，立刻点击目标页上的按钮应正常响应**（此前约 400ms 内被吞；设备端计时 0.1s 已复验）。
- [ ] 页签直达（点底栏入口回主页）重置副页时不补播缩小动画。
- [x] 深链／通知／跳转**直接打开**子页时从底栏图标放大展开（Q2 已定）：设备端以 `am start --ez open_quick_capture true` 冷启动直达收集箱复验——子页正常展开、底栏标签切为「收集箱」、点今日入口回主页正常。
- [ ] 更新说明「查看版本路线图」与课程编辑器「管理地点与出行参数」两处跳转同样播放大动画（同一条规则，未单独跑）。
- [x] 子页放大/缩小转场不掉帧：修复前后各测一次（重帧 17.5% → 4.2%，连续 20–24ms 段落消失；见 `534bc8d`）。
- [ ] 底栏选中底色从上一页签滑到当前页签；进入子页变圆环；标签交叉淡入替换为子页名；切页签不触发回弹。
- [ ] 回退／折返：有历史时两端圆瓣伸出（260ms），无历史时收回不占位。
- [ ] 设置 → 外观 → 动画速度：关闭／较快／标准／较慢四档全部生效（含底栏回弹与抑制窗口）；关闭时所有转场瞬时完成。

## 返回与页面历史

- [ ] 今日页按返回：提示「再按一次返回键退出应用」，4 秒内再次返回才退出；窗口外再按只重新提示。
- [ ] 冷启动后立即按返回仍走二次确认，不直接退出。
- [ ] 「不再提示」永久生效；设置 → 退出确认 可重新开启。
- [ ] 非今日页按返回先回今日主页（进历史）。
- [ ] 回退／折返可跨页签回到刚才页面并恢复状态；长按回退键弹历史列表，点选直达并截断其后历史（含收起动画）。
- [ ] 导航历史（2026-09-10 按用户反馈放宽）：任何页面切换都记为一步（含点底栏入口切主页），「上一步」能回到刚才的页面并恢复子页状态；A→B→A 去抖；容量 30；后台遗留子页不影响判定。

## 弹窗与更新

- [ ] 含输入的弹窗被误关后重开恢复内容；新建类弹窗「清空」回到空白并清缓存，取消保留草稿。
- [ ] 覆盖安装后更新提示只显示一次，展示 8.1 的 2–3 条核心变化，不落兜底文案。
- [ ] 检查更新：默认只收正式版；勾选「接受候选版」需二次确认；同日自动检查去重。
- [x] 弹窗打开期间底栏可点（页内浮层，2026-09-09 设备复验）：底栏节点与弹窗节点同时出现在无障碍树；弹窗开着点「计划」成功切页。
- [x] 弹窗关闭路径：返回键关闭、点遮罩关闭、点弹窗卡片空白处**不**关闭（不穿透）；关闭后历史键可继续使用。
- [x] 键盘弹出时弹窗整体上移到键盘与底栏之上，标题／输入框／按钮完整可见（快速记录弹窗实测）。
- [x] 弹窗覆盖：任意页面的弹窗（含设置子页、计划子页、课程编辑）外观一致、无裁切；四套主题下卡片对比度正常。
- [x] 弹窗打开期间点入口／上一步／下一步／＋：**先关闭弹窗再导航**（2026-09-10 按用户反馈修正；此前"弹窗保留"会让页面切走而弹窗浮在新页面上、且这次切换不进历史）。
- [x] 横屏 3168x1440：卡片 132..1040、底栏顶 1040，按钮不再被底栏盖住（`4fcac69` 修复）；弹窗开着仍可切页签。（应用现已锁定竖屏，此项作为大屏/分屏被忽略锁定时的兜底保留）
- [x] 窄屏 1080x2340、大字体 1.3x、深色模式、三键导航：底栏图标与文字无裁切重叠、弹窗完整（设备复验）。
- [x] 弹窗打开时底栏亮起一圈主题色柔光（不再压暗；2026-09-10 按用户反馈从"同档压暗"改为"仿照亮"），底栏仍可点。
- [x] 弹窗卡片上下平移进出屏幕（0.32×屏高），遮罩仍淡入淡出。
- [x] 子页放大/缩小转场掉帧：修复前后各测一次（17.5% → 4.2% 重帧，连续 20–24ms 段落消失）。

## 数据与兼容

- [ ] 新增存储键（动画速度、退出确认、接受候选版）默认值正确；旧存档读取不报错、不改写其他键。
- [ ] 任务、历史、目标、课程、地点、主题、权限与通知渠道在覆盖安装后不变。
- [ ] 损坏数据备份失败时仍暂停写入并提示。

## 目标设备（OPPO / ColorOS 16 / Android 15）

- [x] 通知与后台：`日程与活动提醒 → 1 分钟后测试通知`，点完立刻回桌面，**57 秒后**收到通知（channel `focusflow_task_reminders`，importance 4）；两次实测分别在 00:47:03、00:51:12 到达，应用自身诊断显示"后台实测：最近一次按时送达"、电池后台"不受电池优化限制"、当前使用精确提醒。
- [x] 重启恢复（机制）：`force-stop` 后 FocusFlow 的闹钟被系统清空（0 条），重新启动应用后同一 `STATUS_CHECK_IN` RTC_WAKEUP 闹钟自动重新注册（origWhen 不变）——这正是 `BOOT_COMPLETED` 走的那条恢复路径。
- [x] 真机重启实测（2026-09-10，维护者授权）：重启 25s 后开机完成，但 55s 时应用进程与闹钟均未恢复；`dumpsys activity broadcasts` 显示 ColorOS **推迟**了开机广播给本应用（`DEFER_BY_OPLUS SUB_REASON: PRESSURE` / `DEFERRED OFFLOAD for manifest com.sakata.focusflow.BootReceiver`）。手动打开一次应用后闹钟立即恢复。
- [x] 据此修正应用内表述（原来写"重启后自动恢复"过强）：设置页与说明书改为"收到开机广播会重新登记；厂商推迟时打开一次应用即可恢复"，并给出 OPPO／一加／realme 允许自启动的具体路径。
- [x] 推迟检测端到端复验（2026-09-10 第二次重启）：重启前 `BOOT_COUNT=16`，重启后 `17`、应用进程为空、闹钟未恢复；打开应用后闹钟恢复，提醒页出现红色提示"本次开机时系统推迟了开机启动"。设置页文本已按"过长收束"压成一行，细节放问号帮助与说明书。
- [x] 手势导航与三键导航下底栏形变、圆瓣与悬浮键不遮挡、可点击（设备复验：两种模式布局一致）。
- [x] 窄屏／大字体／深色模式下底栏横向滚动正常，选中色块与图标对齐（窄屏 1080x2340、字体 1.3x 复验无重叠）。
- [x] 四套主题逐一对比度：选中色块 vs 底栏底色 海盐蓝 5.09 / 薄荷绿 5.00 / 暖杏 4.66 / 暮紫 5.26（均 ≥4.5:1）；标签文字 17.1–17.5:1。深色模式默认主题 4.76:1。
- [x] 竖屏锁定：设备横屏时窗口仍为竖屏（`requestedOrientation=SCREEN_ORIENTATION_PORTRAIT`）；分屏按用户决定不考虑。
- [ ] 至少一天连续使用：无幽灵提醒、重复提醒、闪退；无转场残留图层。

## 发布收尾

- [ ] VERSIONING「当前版本」、README、AGENTS、CHANGELOG、RoadmapData、`updateHighlightsFor` 全部同步到 8.1.0。
- [ ] `versionName` 改 `8.1.0`、`versionCode` 再 +1，重跑全部门禁。
- [ ] 补 `docs/8.1.0-release-notes.md` 与 `release-8.1.0.yml`（锁定已验收 run 与 APK 哈希），维护者批准后合并、打 tag `v8.1.0`、建 GitHub Release。
