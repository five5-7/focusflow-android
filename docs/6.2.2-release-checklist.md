# FocusFlow 6.2.2 发布候选检查表

版本：`6.2.2` / versionCode `464`

## 自动验证

- [ ] `TaskReminderPolicyTest` 覆盖提前窗口、到点提醒、零提前量、关闭提醒与精确权限降级。
- [ ] 后台测试策略覆盖等待、按时、延迟与超时状态。
- [ ] GitHub Actions 单元测试通过。
- [ ] GitHub Actions Android APK 编译通过。
- [ ] 使用既有稳定签名生成 APK，未回退到不兼容的 debug 签名。

## 真机验证

- [ ] 设置提前 5 分钟，将任务安排到至少 8 分钟后。
- [ ] 退回桌面且不清理后台，提前 5 分钟收到“即将开始”。
- [ ] 保持后台，到点再次收到“到点了”。
- [ ] 在提前提醒出现后改期，旧到点不再通知，新时间的提前与到点均生效。
- [ ] 在提前提醒中标记完成，原到点不再通知。
- [ ] 设置提前 0 分钟时只收到一次到点提醒。
- [ ] 设置诊断分别显示下一次提前提醒和下一次到点提醒。
- [ ] 电池优化状态与系统设置一致；测试延迟到打开应用才送达时，诊断显示“延迟”而不是“成功”。
- [ ] 强唤醒测试在桌面和锁屏状态均按时通知，不主动播放闹钟声音；记录设备是否显示闹钟标识。

## 发布边界

- [ ] 未经用户明确确认不合并 PR。
- [ ] 未经用户明确确认不创建 `v6.2.2` Release。
