# 8.2.0 外观系统 · 交接材料（供新对话直接起步）

> 维护者要求：「准备新对话需要的材料」。本文是**新会话的唯一入口**：先读本文，再按需展开
> `docs/8.2.0-appearance-plan.md`（设计与实测记录）与 `docs/8.2.0-checklist.md`（逐条验收清单）。
> 生成时间：2026-09-10 17:40；**17:55 更新**：第 7 项（自定义主题工具升级）已完成，见第三节 B-1。

**交接时的验证状态**：工作区干净；`:app:testDebugUnitTest` 复核
**95 个测试类 / 576 条 / 0 失败 / 0 跳过**；`:app:assembleRelease` 成功出签名包。
本轮新增的是第 7 项实现（外观控件复用、实时预览、预设可带整套外观、恢复默认连外观复位）、
「渐变跟随内容」的开关自述行、以及**离线对比度总账**（7 主题 × 浅/深 × 4 档背景，见下），
没有改任何既有外观的默认行为。

**对比度证据已经离线算全**：`AppearanceContrastMatrixTest`（`app/src/test/.../`）用渲染同一批纯函数
算出每一档背景在**最不利底色**上的正文对比度，结果表写在 `docs/8.2.0-appearance-plan.md` §六「阶段 C/D」，
明细文件是 `app/build/reports/appearance-contrast-matrix.md`。全表最低 **6.9:1**、不含图片最低 **8.1:1**，
所以真机上只需要抽查几个点与表对照——**掉帧（`gfxinfo`）才是真机唯一必须现场取的数据**。

## 一、五分钟上手

| 项 | 值 |
|---|---|
| 代码仓库 | `D:\focusflow\focusflow-android-8.0.0`（**不是** `focusflow-android`，那是最老的 6.0 checkout；也不是 `focusflow-android-6.4`） |
| 分支 / HEAD | `agent/focusflow-8.0.0` / `e4d54ea`，工作区干净 |
| 与 main 的差距 | 84 个提交、81 个文件、+7819 / −1433 |
| 当前版本号 | `versionCode 521` / `8.1.1`（**8.2.0 尚未分配版本号**，见第八节） |
| 构建脚本 | `powershell -NoProfile -ExecutionPolicy Bypass -File D:\focusflow\.tmp\build.ps1`（默认跑 `:app:testDebugUnitTest :app:assembleRelease`；用 `-Tasks` / `-Log` 改） |
| 真机 | OPPO `3B1F5EEADK1UAH74`，ColorOS 16 / Android 15，1440×3168 |
| 真机时段 | **每天 17:00 之后不做真机测试**（维护者约定）；本轮 17:32 开工，所以只做离线工作 |

### 必读的坑：git 要带 `safe.directory`

本机 Windows 用户 SID 与仓库属主不一致，直接 `git ...` 会报
`fatal: detected dubious ownership`。所有 git 命令都要加前缀：

```powershell
git -c safe.directory=D:/focusflow/focusflow-android-8.0.0 -C D:\focusflow\focusflow-android-8.0.0 status
```

写入 `~/.gitconfig` 需要提到工作区外权限，直接带 `-c` 最省事。

## 二、目标（7 项）与当前完成度

维护者口径：**全部可选**；原有 4 套内置主题保持现状不变；老装机默认外观不变；不破坏数据契约。

| # | 特性 | 状态 | 证据 / 缺口 |
|---|---|---|---|
| 1 | 页面与卡片渐变 | ✅ 已实现 | 页面渐变 4 档（跟随主题 / 主题渐变 / 固定色 / 图片）＋卡片材质渐变；单测 `SurfaceMaterialTest` 断言"渐变可感知（≥8 级）""最强档正文 ≥7:1" |
| 2 | 自导入图片背景 + 可调透明度 | ✅ 已实现 | `AppearanceImages` 私有目录降采样存储；透明度 0–100 滑杆；真机已验 12% / 97% 两档，97% 偏冷蓝 |
| 3 | 从导入图片自动抽取主题色 | ✅ 已实现 | `PaletteExtractor` + `ExtractedTheme`，纯函数带单测；太素时提示且不改主题；真机已验"变蓝" |
| 4 | 卡片材质（柔光／纸感；毛玻璃待实测） | ⚠️ 部分 | 默认／渐变／柔光／纸感四档已实现（纸感噪点程序生成、零包体积）；**毛玻璃未实现**，等第四节帧时间结论 |
| 5 | 课表底色可导入图片或选色 | ✅ 已实现 | 只动底板，课程块/日程块颜色来自数据、一行没碰；真机采样验证过 |
| 6 | 新增更多内置主题 | ✅ 已实现 | 4 原有 + 3 新增（GRAPHITE / SAKURA / BAMBOO）= 7 套；`ThemeGalleryTest` 冻结原有 4 套金标 |
| 7 | 升级自定义主题工具 | ✅ 已实现（待真机走查） | 新增实时预览（走真实渲染）、外观控件与「设置 → 外观」共用同一份实现、预设可连整套外观一起存（老预设行为不变）、恢复默认连外观复位；`ThemePresetCodec` 7 条单测 |

横向要求「同步应用内文案」：说明书第八章、帮助、快速上手、路线图、CHANGELOG 的 8.2.0 小节**都已写完**（提交 `0c94d8c`），`UserGuideContentTest` 守 11 个关键词。

## 三、未完成清单

### A. 必须上真机（17:00 前做）

> **2026-09-10 晚已完成**：①「渐变跟随内容」**确认生效**（关＝滚动前后逐像素完全相同、一屏内级差 13.2；
> 开＝同位置随滚动变化、一屏内只有 4.8）；②掉帧四个场景 + 纸感材质**全部 PASS**（0.44%–3.72%，预算 5%）；
> ③深色下页面正常但**渐变幅度偏小（2–3 级）**，维护者已拍板为设计选择。
> 明细：plan §六「阶段 E」、清单第三/四节。
>
> **2026-09-10 20:00 追加完成**：④**默认外观与 8.1.1 逐像素比对已跑通**——
> `reset_appearance.ps1` 复位 + `invariant_check.ps1` 一条命令自动来回装包比对，
> `meanAbsDiff 0.37` PASS，且**整页上半部分逐像素 0.00**（差异只在底栏，是本版要求的描边改款）。
> 详见清单 §一。
> **仍未做**（都不阻塞发版）：逐档截图与采色、深色四档截图、深色闪白录屏、第 7 项排版走查。

按 `docs/8.2.0-checklist.md` 逐条走。原先列的三条里，第 1、2 条**已完成**，只剩第 3 条：

1. ~~**不变量**：默认外观与 8.1.1 逐像素一致~~ → **✅ 已完成，见清单 §一**。
   **不要**再去比几天前的旧图（下面这条命令里的 `j1_811_plan.png` 已过期，内容不一致会得到 20+ 的假差异）；
   现在的正确做法是先 `reset_appearance.ps1` 复位，再 `invariant_check.ps1` 自动来回装包截图比对。
   ```powershell
   # 旧的单张比对（已废弃，仅留作对照）
   powershell -File D:\focusflow\.tmp\pixdiff.ps1 -A D:\focusflow\.tmp\j1_811_plan.png -B D:\focusflow\.tmp\j2_new_default_plan.png
   ```
2. **「渐变跟随内容」到底生效没有** —— **✅ 已完成**（提交 `cfbfaab` 换了实现：渐变改由滚动内容按
   **内容高度**铺，不再依赖滚动事件；真机确认滚动时同位置底色变化、一屏内级差 4.8 vs 固定模式 13.2）。
   下面这条取样命令仍可用于复核其它渐变档位。
   ```powershell
   powershell -File D:\focusflow\.tmp\scan_col.ps1 -Img D:\focusflow\.tmp\y1_set_top.png -X 20 -From 300 -To 2600 -Step 100
   ```
3. **深色模式四档背景 + 无闪白**（维护者明确要求过的项）：`docs/8.2.0-checklist.md` §三。

其余真机项：七套主题各一张、三种卡片材质、课表底色三档、箭头位置确认、`gfxinfo framestats` 基线
（页签切换 / 副页转场 / 长列表滚动 / 图片背景 / 弹窗开合；8.1.1 基线：页签重帧 1.7%、副页 4.2%）。

> **对比度不必再逐格取了**：`AppearanceContrastMatrixTest` 已经把 7 主题 × 浅/深 × 4 档背景
> （图片分 1%/50%/100%）全算完（最不利底色），表在 `docs/8.2.0-appearance-plan.md` §六「阶段 C/D」。
> 真机价值最高的是**掉帧数据**——它同时也是"毛玻璃要不要做"的唯一依据。

### B. 不需要真机、新会话可以立刻做

**B-1：把「自定义主题工具」升级成整套外观的一站式工具（目标第 7 项）—— ✅ 已完成（2026-09-10 晚）。**

已落地（`docs/8.2.0-appearance-plan.md` §7.7 有完整设计说明）：

- 抽出可复用控件 `PageBackdropControls` / `GradientStopsControls` / `CardMaterialControls` /
  `TimetableBaseControls`（都在 `AppearanceSettings.kt`），「设置 → 外观」与自定义主题工具**共用同一份实现**；
- 新增 `AppearancePreview.kt`：预览用真正的 `focusFlowThemeSpec` + `Modifier.appearanceBackdrop` +
  `FocusCard` 画迷你页面 —— 预览与应用后是同一段渲染代码；
- `ThemePreset` 增加可选 `appearance`；新增 `ThemePresetCodec`（纯函数 + 7 条单测），
  **老预设（只有 name+colors）读出 `appearance == null`，应用它不动背景与卡片**；
  写回时也不会凭空多出 `appearance` 键；
- 预设里引用的图片被删 → `AppearanceSpec.withExistingImages` 降级为跟随主题；
- 「恢复默认」连外观一起复位（`MainActivity.onRestoreDefaultTheme`），按钮文案同步改为
  「恢复默认主题配色与外观」；
- 应用内文案同步：`HelpContent`（外观 + 自定义主题两块）、`UserGuideContent` 第八章、
  `RoadmapData` 8.2.0、`CHANGELOG.md` 8.2.0。
- 验证：`:app:testDebugUnitTest` 94 类 / 571 条 / 0 失败；`:app:assembleRelease` 成功出签名包
  （7,803,050 B；`versionCode` 仍是 521 / `8.1.1`，方便覆盖安装做 A/B）。
  **可直接装的包**：`D:\focusflow\.tmp\FocusFlow-8.2.0-dev.apk`
  （当前 SHA-256 `71926c6b…51212`；每次构建因签名时间戳不同，哈希会变，以文件大小与"装上去版本号是 8.1.1"为准）。
  已核验 dex 里确实含新代码（`ThemePresetCodec` 与「恢复默认主题配色与外观」文案）。
- **仍待真机走查**：预览块 176dp 排版、预设行多一行说明、抽色只改工作副本的直觉性、
  老预设应用后背景不被改动、恢复默认的复位范围。

**B-2：给设置页加"当前生效状态"自述行—— ✅ 已完成（2026-09-10 晚）。**
「渐变跟随内容」开关下面现在有一行「当前：跟随内容（渐变铺满整段内容，每屏只走一小段）／固定一屏（整条渐变正好一屏）」，
由 `AppearanceSpec.gradientSpanLabel()` 生成（纯函数 + 单测）。真机上直接截图这一行即可判定开关生效情况，
不再需要用像素反推——这正是第三节 A-2 之前缺的那块证据。

**B-3：卡片材质收编剩余 19 处**（`ElevatedCard` 13、`OutlinedCard` 1、`Card(border=…)` 1、`Card(onClick=…)` 1、
无显式底色的 `Card` 1、有意保留的课表/日程表底板 2）——已收编 32 处，收编口径与剩余原因见
`docs/8.2.0-appearance-plan.md` §7.4。**真机 A/B 之前不要动**：这五类改了就不是逐像素一致
（会丢 1dp 阴影 / 描边 / 可点击重载）。做法：同一页面同一路径，8.1.1 包与当前包各截一张，`pixdiff.ps1` 比。

**B-4：技术债——滚动相位那套代码已经是死代码 —— ✅ 已完成（2026-09-10 晚，提交 `6f33d0e`）。**

先做了一次独立的静态可达性核查，结论与交接时的判断完全一致（交接里引用的行号全部准确）：
`appearanceBackdrop` 的 `Page + GRADIENT + 跟随内容` 分支提前 `return this`，**这个 return 本身是必需的**
（它把渐变交给 `ScrollableWithBar` 按内容高度铺），所以 `phase` 恒为 0、`window` 恒为 1，
`windowStops` 永不执行。已删除：`gradientAt` / `windowStops` / `GRADIENT_SCROLL_SPAN` /
`accumulateScrolledScreens`、`ThemeGradient.page` 的 `phase`+`window` 两个参数、
`appearanceBackdrop` 的 `scrolledScreens` 参数、`MainActivity` 的
`scrolledScreens` / `scrollTracker` / `.nestedScroll(...)` 挂载，以及对应的 5 条单测
（净删 174 行、增 57 行）。`page()` 补了一条替代断言（三站、方向、强度 0 = 纯色、自选端色原样落位）。

**未动的数据契约**：`appearance_gradient_follows` 偏好键、预设 JSON 的 `gradientFollows`、
`AppearanceSpec.gradientFollowsContent` —— 开关仍然有效。
**验证**：单测 95 类 / 575 条全绿；重跑不变量比对 `meanAbsDiff 0.37`（清理前 0.58），
证明是纯粹的渲染等价重构。真要回退"按滚动量取窗口"，历史在 `cfbfaab` 之前。

**顺带修掉一个连带缺陷（提交 `852dfe1`）**：开关打开后 `appearanceBackdrop` 提前 return，
而自定义主题的预览块不是滚动容器，于是预览会退化成一片纯色 ——
与说明里"改渐变/材质时预览当场变化"相矛盾。现在预览按与 `ScrollableWithBar` 相同的口径
自己补一张撑满预览高度的渐变。**注意不要靠删掉那个提前 return 来"修"它**，
那会让真实页面重新画上视口渐变，正好破坏「跟随内容」的效果。

**B-5：发版材料 —— ✅ 已完成（2026-09-10 晚）。**

- `docs/8.2.0-release-notes.md` 新建（本版做什么、主要变化、不变量实测、真机验收记录、数据契约、安装验证）。
- `app/build.gradle.kts`：`versionCode 522` / `versionName "8.2.0"`。
- 同步：`CHANGELOG.md` 8.2.0 小节（去掉"开发中"、补顶角圆角与不变量实测、更新测试条数）、
  `VERSIONING.md` 当前版本段、`RoadmapData.kt` 的 8.2.0 条目从 `CANDIDATE` 改为 `DONE`。
- 应用内更新说明 `QuickStartDialog.updateHighlightsFor` 的 8.2.0 分支本来就有 3 条可感知变化，
  未落兜底文案，无需改动。
- **下一步只剩维护者动作**：打 `v8.2.0` tag、建 GitHub Release、推资产
  `FocusFlow-8.2.0.apk`。按 AGENTS.md，合并与发版都需要维护者明确指示。

**关于"不变量"的准确口径（2026-09-10 追查后修正，别再用旧说法）。**
跑通后 `meanAbsDiff=0.37`，但**不要**把这 0.37 说成"动画残帧"——逐区域查下来：
**整页上半部分（y<2600）是 0.00，差异 100% 在底栏**，成因是 8.1.1 的
`BorderStroke(2.dp, alpha 0.40)` 硬灰线 vs 本版 `BorderStroke(0.6.dp, alpha 0.12)` + `shadowElevation 7dp`。
这是提交 `cc394cd` 按维护者口径（「不要那条 2dp 的硬灰线」）做的改动，**在 8.1.1 发版之后**，
属于 8.2.0 的既定范围。所以准确说法是：**"外观系统在默认档位下不改变任何渲染"**
（页面上半部分逐像素 0），底栏那点是专门要求的描边改款。
底栏几何未变（仍是 x=64..1376、y≈2672..3071）。

## 四、数据契约（只增不改）

`PrototypeStore` 的 preferences，全部带默认值，老装机读不到 = 现在的样子：

| 键 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `appearance_page_backdrop` | String | `theme` | `theme` / `gradient` / `color` / `image` |
| `appearance_page_image` | String | `""` | 私有目录相对文件名 |
| `appearance_backdrop_opacity` | Int | `100` | 背景图不透明度 0–100 |
| `appearance_gradient_strength` | Int | `100` | 渐变强度 0–200（%） |
| `appearance_page_color` | Int | `0` | ARGB，0 = 未选（固定背景色） |
| `appearance_gradient_follows` | Boolean | `false` | 渐变跟随内容 |
| `appearance_gradient_top` / `appearance_gradient_bottom` | Int | `0` / `0` | 自选渐变停靠色，0 = 跟随主题 |
| `appearance_card_material` | String | `tonal` | `tonal` / `gradient` / `soft` / `paper` |
| `appearance_timetable_backdrop` | String | `theme` | `theme` / `color` / `image` |
| `appearance_timetable_color` | Int | `0` | ARGB，0 = 跟随主题 |
| `appearance_timetable_image` | String | `""` | 同上 |
| `appearance_timetable_opacity` | Int | `100` | 底图不透明度 |
| `appearance_extracted_colors` | String | `""` | `primary;secondary;tertiary` 十六进制 |

预设存档（键 `theme_presets`，8.2.0 起元素形如 `{name, colors, appearance?}`）：`appearance` 可缺，
缺了表示"这套预设只管配色"，应用它不动当前的背景/卡片；写回时不会给老预设补上这个键。
`AppearanceSpec.withExistingImages` 负责"预设里记的图片已被删除"时的降级（→ 跟随主题）。

其它不可动摇的约定：

- 图片只存在 `filesDir/appearance/`，导入时降采样到 ≤1440×3168 再另存；切换主题/背景**不删**用户图片；
  不引入网络能力。
- **课程块颜色与 `FocusFlowSchedulePalette` 是数据契约**：外观只改课表**底板**。
- **原有 4 套内置主题的颜色字面量已冻结**（维护者要求），`ThemeGalleryTest` 里有金标；
  遇到"某套配色对比度不达标"时，改的是**判定口径**（强调色按 AA Large 3:1），不是颜色。

## 五、代码地图（`app/src/main/java/com/sakata/focusflow/`）

| 文件 | 作用 |
|---|---|
| `AppearanceSpec.kt` | 外观数据模型；`BackdropKind` = THEME/GRADIENT/COLOR/IMAGE；`CardMaterial`；`fromKeys(...)` 14 参 |
| `SurfaceMaterial.kt` | **所有外观的唯一渲染出口**；`LocalAppearance` / `LocalBackdropBitmap`；`pageContainerColor()` / `timetableContainerColor()`；`ThemeGradient`（page/card/timetable + 8 组自选配色）；`appearanceBackdrop(...)`；`scrimAlpha`；`drawImageCover`；`windowStops` / `gradientAt` / `accumulateScrolledScreens`（`GRADIENT_SCROLL_SPAN≈3.2f`）；`Modifier.pageLayerBackground()` |
| `FocusCard.kt` | 统一卡片：TONAL 走原生 `Card`（逐像素不变），其余走 `cardMaterialFill` |
| `AppearanceSettings.kt` | 外观控件（**可复用**）：`PageBackdropControls` / `GradientStopsControls` / `CardMaterialControls` / `TimetableBaseControls`；`AppearanceSettingsSection` 只是把它们按顺序摆一遍 |
| `AppearancePreview.kt` | 自定义主题工具里的实时预览：真 `ColorScheme` + `appearanceBackdrop` + `FocusCard` 搭迷你页面 |
| `CustomThemeEditor.kt` | 自定义主题工具：预览 → 对比度体检 → 六色槽 → 外观控件 → 恢复默认 → 预设（可带整套外观） |
| `ThemePresetCodec.kt` | 预设编解码（纯函数 + 单测）：`appearance` 可选、老存档兼容 |
| `PaletteExtractor.kt` / `ExtractedTheme.kt` | 抽色（纯函数，可复现）+ 派生完整 scheme |
| `AppearanceContrast.kt` / `ThemeContrastAudit.kt` | WCAG 对比度守卫 / 六对体检项 |
| `AppearanceImages.kt` | 私有目录存取、降采样、删除 |
| `AppTheme.kt` | 7 套内置主题 + CUSTOM；`deepen()` 浅色页底加深 5% |
| `ScrollableContainers.kt` | `ScrollableWithBar`：跟随内容模式下渐变**画在滚动 Column 上** |
| `MainActivity.kt` | 外观状态、根背景 Box、`Scaffold(containerColor = pageContainerColor())`、抽色后切 CUSTOM 主题 |
| `SubpageMotion.kt` / `StatusBarScrim.kt` | 副页层不透明背景（`pageLayerBackground`）/ 状态栏取渐变顶色 |

单测（`app/src/test/.../`）：`AppearanceSpecTest`、`AppearanceContrastTest`、`SurfaceMaterialTest`、
`PaletteExtractorTest`、`ExtractedThemeTest`、`FocusCardMaterialTest`、`ThemeGalleryTest`、
`ThemeContrastAuditTest`、`ThemeColorsCodecTest`、`ThemePresetCodecTest`、`NavigationHistoryTest`、
`NavigationThemeTest`、`UserGuideContentTest`。

预设 JSON（键 `theme_presets`）的形状也属于数据契约：数组元素是
`{name, colors, appearance?}` —— **`appearance` 可缺**，缺了就是"这套预设只管配色"。
老版本读自己的存档不会多出这个键（`ThemePresetCodecTest.colourOnlyPresetsDoNotGainTheField` 守着）。

## 六、验证工具（都在 `D:\focusflow\.tmp\`）

```powershell
# 单测 + 出包（签名变量已内置在脚本里，密码从 D:\focusflow\signing\passwords.txt 读）
powershell -NoProfile -ExecutionPolicy Bypass -File D:\focusflow\.tmp\build.ps1
# 只跑单测
powershell -NoProfile -ExecutionPolicy Bypass -File D:\focusflow\.tmp\build.ps1 -Tasks ":app:testDebugUnitTest" -Log D:\focusflow\.tmp\t.log

# 截图 / 取色 / 比对
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" exec-out screencap -p > D:\focusflow\.tmp\a.png
powershell -File D:\focusflow\.tmp\sample_px.ps1 -Img a.png -Points "20,300;20,1100;720,2872,底栏"
powershell -File D:\focusflow\.tmp\pixdiff.ps1 -A a.png -B b.png -Region "0,200,1440,2600"
powershell -File D:\focusflow\.tmp\histogram.ps1 -Png a.png
powershell -File D:\focusflow\.tmp\scan_col.ps1 -Img a.png -X 20 -From 300 -To 2700 -Step 100
# UI 树（不稳，会超时；用于拿控件 bounds）
powershell -File D:\focusflow\.tmp\uitree.ps1 -Out D:\focusflow\.tmp\ui.xml
# 掉帧测量（第 4 节要的数字）：自动 reset → 提示做手势 → 取数 → 打印 janky% 与 p50/p90/p99 → 按 5% 预算给退出码
powershell -File D:\focusflow\.tmp\framestats.ps1 -Label tabs
powershell -File D:\focusflow\.tmp\framestats.ps1 -FromFile D:\focusflow\.tmp\gfx_fs4.txt   # 离线复核已有 dump
# 让视觉模型描述截图（SiliconFlow Qwen3-VL，key 在 sf_key.txt）
node D:\focusflow\.tmp\ask_vision.js a.png D:\focusflow\.tmp\vision_prompt.txt D:\focusflow\.tmp\vision_out.txt

# 不变量比对前**先复位**：把主题→海盐蓝、页面背景→跟随主题、卡片材质→默认、课表底色→跟随主题。
# 设备被真机试验弄脏过，不复位就会得到假差异。跑完会打印实际状态并截图 reset_ui.png。
powershell -File D:\focusflow\.tmp\reset_appearance.ps1
powershell -File D:\focusflow\.tmp\reset_appearance.ps1 -Verify   # 只看当前状态，不改
powershell -File D:\focusflow\.tmp\reset_appearance.ps1 -DryRun   # 只打印计划
```

> **这两个脚本别用管道接输出**（`$out = powershell -File ... | ...`、`2>&1` 都算）：
> adb 会 fork 一个 server 进程并继承 stdout 句柄，脚本跑完进程也不退出，调用方一直等 EOF。
> 实测同一份 `-Verify`：**重定向到文件 38.3 秒正常退出**，用管道则挂住不返回。
> 要抓输出就用 `Start-Process -RedirectStandardOutput <file> -PassThru` 再 `WaitForExit(90000)`。
> （注意重定向出来的文件是 ANSI，中文会花掉，别据此判断脚本好坏——以脚本自己的退出码/截图为准。）

真机几何（1440×3168）：页签 x≈246/482/956/1194，图标 y≈2832，底栏容器 ≈[64,2672]-[1376,3072]，
新增按钮 (720,2872)。屏幕黑屏时先 `input keyevent KEYCODE_WAKEUP`。
底栏顶角圆角量法：完整胶囊半径 = 栏高一半；`10dp = 40px`、`16dp = 64px`（density 4）。
注意底栏填充色 `rgb(225,235,238)` 与部分卡片撞色，且 7dp 阴影会糊住顶角，
"从左往右找第一个柱色像素"会系统性偏内 —— 量圆角要配合紧阈值并从已知柱体内部反扫。

## 七、踩过的坑（省时间用）

1. **`.ps1` 里有中文且没有 BOM → PowerShell 5.1 会乱码并解析失败。** 工具脚本一律 ASCII-only；
   要写中文用 `[System.IO.File]::WriteAllText($p, $s, (New-Object System.Text.UTF8Encoding($false)))`。
   中文界面文案放进 `patterns.txt` 用编号引用。
2. `.NET` 的工作目录与 PowerShell 的 `cd` 不一致 → 脚本里用**绝对路径**。
3. Gradle daemon 会抓住调用者的管道不放 → `build.ps1` 里是在 `cmd /c` 内重定向到日志文件的，别改。
4. `Get-CimInstance Win32_Process` 被沙箱拒绝；`npm view` 需要 `--cache D:\focusflow\.tmp\npmcache`。
5. `uiautomator dump` 时好时坏，读开关状态不可靠——不要用 dump 判断 Switch 状态。
6. `Compose` 的 `lerp(Color, Color, Float)` 是**分量级 sRGB 插值**（真机实测过），不是线性空间。
7. `Modifier.background(Brush)` 的渐变**跨元素自身高度**——这正是「跟随内容」的实现依据。
8. 转场透明是踩过的老坑：副页每个动画层都必须有**不透明**背景（`pageLayerBackground`），
   开启渐变/跟随内容时也不能破例（提交 `e4d54ea` 就是补这个）。
9. **`function X { param([string[]]$Args) ... $y = & $tool @Args }` 一定不要这么写。**
   `$Args` 是 PowerShell 的自动参数，用它当形参名会让 `@Args` splat 失效：命令收不到任何参数，
   `adb` 于是打印一大段 help、**退出码仍是 0**，后面的 `uiautomator dump` / `pull` 全部没执行，
   脚本继续读**上一次留下的 XML** 并据此报告页面状态。症状是"脚本说得头头是道，但全是假的"，
   2026-09-10 为此丢了近一小时。改用 `param([string[]]$AdbArgs)` + `@AdbArgs`。
   配套口径：先删本地 dump 再拉，拉不到就返回空串报错，**绝不**回退读旧文件。
10. **`uiautomator dump` 会冻结 UI 大约 2–5 秒，紧接着发的 `input tap` 有时会被丢掉。**
   所以脚本里"dump → 按坐标点一下 → 认为已经跳转"是不可靠的；每一步点完都要**重新 dump 验证页面**
   （`reset_appearance.ps1` 的 `Open-Appearance` 就是带重试的写法，最多 3 轮）。
11. **`$ErrorActionPreference = "Stop"` 与 adb 不兼容**：adb 把 daemon 启动横幅和传输进度写在
   stderr，`Stop` 会让每一条都变成终止错误。用 `Continue`，再靠"校验每步输出"来发现真正的失败。
12. 判断"页面到底在哪"不要只看一个关键词：**今日页的 dump 里也含"深色模式"**。
   外观页要同时含 `页面背景` + `深色模式`。
13. 读 `adb` 抓下来的 XML 别用 `Get-Content -Raw`（中文会变乱码）；
   用 `[System.Text.Encoding]::UTF8.GetString([System.IO.File]::ReadAllBytes($p))`。
14. **主题行与卡片材质的可读性不一样**：主题行的选中态会渲染一个 `已选择` 文本节点，能从 dump 读出来
   （"已选择"前面最近的那个主题名就是当前主题）；而 `默认/渐变/柔光/纸感` 四个 FilterChip
   只靠填充色区分，dump 里四个都是 `selected="false"`，**读不出来**，别浪费时间去解析。
15. **内置主题的 label 不等于它的名字**：`OCEAN` 的 label 是 **海盐蓝**（不是"海洋"），
   `MINT` 是"薄荷绿"。脚本里按 label 找行，写错就永远找不到、静默跳过（第 9 条的同类事故）。
16. **设备不是干净的**：`.tmp` 里大量真机试验会留下非默认外观（2026-09-10 19:00 实测残留
   `主题渐变 200%` + `薄荷绿`）。跑不变量比对**之前**先跑
   `powershell -File D:\focusflow\.tmp\reset_appearance.ps1` 把主题/背景/材质复位到 8.1.1 的样子，
   否则会拿到假差异。

## 八、发版状态

- **已发布**：`8.1.1`（versionCode 521），GitHub Release 标 Latest，资产 `FocusFlow-8.1.1.apk`
  7,753,898 B，SHA-256 `75533febddda016aa033ef0d255a86c476a1af69b24e8d3fdf223d95982551d6`，
  tag `v8.1.1` → `c3c336f`。仓库是 **`five5-7/focusflow-android`**。
- **8.2.0 还没分配版本号、没建 tag、没写 release notes**。发版前置条件：
  `docs/8.2.0-checklist.md` §一（不变量）全过 + 真机验收记录齐。
- `app/build.gradle.kts` 现在仍是 `versionCode 521` / `8.1.1`，这是**故意**的：本地出的包与 8.1.1
  同版本号，方便覆盖安装做 A/B 对比。

## 九、给新对话的开场建议

直接粘这一段：

> 继续 FocusFlow 8.2.0 外观系统。先读 `D:\focusflow\focusflow-android-8.0.0\docs\8.2.0-handover.md`
> （交接材料）、`docs/8.2.0-checklist.md`（验收清单）、`docs/8.2.0-appearance-plan.md`（设计与实测），
> 然后用 `git -c safe.directory=D:/focusflow/focusflow-android-8.0.0` 确认分支 `agent/focusflow-8.0.0`
> 与 HEAD。现在时间若在 17:00 前就先做真机验收（清单第三节 A 的前三条），否则做离线项
> （B-1 自定义主题工具升级）。构建用 `D:\focusflow\.tmp\build.ps1`。

新会话需要记住的三条底线：**默认外观与 8.1.1 逐像素一致**、**原有 4 套主题颜色冻结**、
**课程块颜色属于数据契约不动**。
